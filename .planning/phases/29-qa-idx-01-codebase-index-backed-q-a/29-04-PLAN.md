---
phase: 29-qa-idx-01-codebase-index-backed-q-a
plan: 04
type: execute
wave: 2
depends_on:
  - 29-01
files_modified:
  - src/server/db/code-index.ts
  - test/code-index-db.spec.ts
autonomous: true
requirements:
  - QA-IDX-01
must_haves:
  truths:
    - "D-12: an incremental refresh deletes the rows for the changed paths and reinserts them, and a full rebuild removes only that repository's rows inside one transaction; retrieval never filters by ref and storage does not grow with history."
    - "D-05: per-file progress is persisted, so a retried build reads the paths already recorded at the in-progress commit sha and skips them instead of re-fetching them."
    - "An interrupted index build resumes without re-fetching files already recorded at the in-progress `indexed_sha`, and a second concurrent build start is coalesced by the `code_index_state` lease rather than producing duplicate rows or a second live build (edge-probe: concurrency)."
    - "Retrieval is scoped to `repository_id`: with two repositories holding chunks that both match a query, a retrieval for one never returns the other's rows."
    - "A file that produced zero chunks — empty, generated, or over the byte cap — is still recorded in the per-file progress table with a skip reason, so a resumed build does not re-fetch it forever."
    - "A pathological chunk (over the byte cap, or a single lexeme past the Postgres lexeme limit) is capped in JS before the insert and does not raise from Postgres."
    - "AUD-01: `last_error` is written already-redacted; the DB module states that the caller routes the message through `redactErrorMessage` because `db/` modules must not import `core/`."
    - "Releasing the build lease also resolves the status: a row still marked `building` becomes `idle`, while a row already `ready` or `failed` keeps that terminal status — so no released row can be read by an operator as a live build, and no release can erase a just-recorded outcome."
    - "The delete accessors are lease-agnostic primitives and say so; sequencing a destructive delete under a held lease is the caller's documented contract, and `truncateCodeIndexForRepo` returns the deleted chunk and file counts for operator observability."
  prohibitions:
    - statement: "MUST NOT issue a table-wide or unscoped delete against the index tables — every destructive statement is bounded to one bound repository_id, so a rebuild or refresh for one repository can never remove another repository's index."
      category: safety
    - statement: "MUST NOT let a released or expired build lease leave a repository presenting as though a build were still running — an operator reading build state must be able to tell a live build from an abandoned one."
      category: transparency
  artifacts:
    - path: src/server/db/code-index.ts
      provides: "refresh, per-file progress, build lease and completion accessors"
      exports: ["deleteCodeIndexChunksForPaths", "truncateCodeIndexForRepo", "listIndexedPathsForSha", "markCodeIndexFileIndexed", "claimCodeIndexBuildLease", "renewCodeIndexBuildLease", "releaseCodeIndexBuildLease", "markCodeIndexBuildCompleted", "markCodeIndexBuildFailed"]
    - path: test/code-index-db.spec.ts
      provides: "ranking, path-tokenization regression, cross-repository isolation, refresh, resumability, lease and pathological-input assertions"
  key_links:
    - from: src/server/db/code-index.ts
      to: src/server/db/client.ts
      via: "the full-rebuild delete plus state reset runs inside queryTransaction, not two separate queryRows calls"
      pattern: "queryTransaction"
    - from: src/server/db/code-index.ts
      to: db/migrations/018_code_index.sql
      via: "code_index_files per-file progress rows keyed (repository_id, path) with indexed_sha"
      pattern: "code_index_files"
---

<objective>
Complete the `src/server/db/code-index.ts` accessor surface with everything the durable build needs — incremental refresh, full rebuild, per-file progress, the build lease, and terminal state — and prove the four DB-level guarantees the rest of the phase assumes: ranking, cross-repository isolation, refresh leaving no orphans, and pathological input not raising.

Purpose: the build Workflow in plan 29-05 is a loop over these accessors. Every correctness property that matters (a resumed build skips finished files; a second build press does not race; a push refresh does not leave stale chunks for a deleted path; one tenant's source never reaches another tenant's answer) is a property of this module, not of the Workflow.

Output: nine additional accessors on the DB module and a DB-backed spec that pins each guarantee against the live test Postgres.
</objective>

<execution_context>
@/home/thomas/repos-ext/codra/.claude/gsd-core/workflows/execute-plan.md
@/home/thomas/repos-ext/codra/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-qa-idx-01-codebase-index-backed-q-a/29-CONTEXT.md
@.planning/phases/29-qa-idx-01-codebase-index-backed-q-a/29-RESEARCH.md
@.planning/phases/29-qa-idx-01-codebase-index-backed-q-a/29-PATTERNS.md
@.planning/phases/29-qa-idx-01-codebase-index-backed-q-a/29-VALIDATION.md
@.planning/phases/29-qa-idx-01-codebase-index-backed-q-a/29-01-SUMMARY.md

<interfaces>
Written by plan 29-01, extended here:

  upsertCodeIndexChunks(env, { repositoryId, path, indexedSha, pathTokens, chunks })
  retrieveCodeIndexChunks(env, { repositoryId, queryExpression, limit }): Promise<CodeIndexChunkHit[]>
  getCodeIndexState(env, { repositoryId }): Promise<CodeIndexStateRow | null>
  markCodeIndexBuildStarted(env, { repositoryId, mode, indexedRef, buildingSha, workflowInstanceId, leaseSeconds })

Migration 018 shapes (plan 29-01):
  code_index_chunks (repository_id, path, chunk_start) PK, chunk_end, content, path_tokens,
                    indexed_sha, search_vector, updated_at
  code_index_files  (repository_id, path) PK, indexed_sha, chunk_count, skip_reason, updated_at
  code_index_state  repository_id PK, status, mode, indexed_ref, indexed_sha, building_sha,
                    indexed_at, file_count, chunk_count, truncated, lease_expires_at,
                    workflow_instance_id, continuation_count, last_error, updated_at

From `src/server/db/client.ts`: queryRows(env, sql, params), queryTransaction(env, fn).
From `src/server/core/audit-redact.ts:75`: redactErrorMessage — called by the CALLER, never here.
</interfaces>
</context>

## Artifacts this phase produces

Phase 29 creates the following; treat every symbol as new rather than as an unresolved reference.

**New files:** `db/migrations/018_code_index.sql`, `src/server/core/code-index.ts`,
`src/server/core/code-index-build.ts`, `src/server/db/code-index.ts`,
`src/server/workflows/index-build.ts`,
`src/client/components/features/repos/code-index-panel.tsx`, and the specs
`test/code-index-tracer.spec.ts`, `test/migration-018-idempotency.spec.ts`,
`test/code-index-splitter.spec.ts`, `test/code-index-chunker.spec.ts`,
`test/code-index-query.spec.ts`, `test/code-index-db.spec.ts`, `test/code-index-tree.spec.ts`,
`test/code-index-selection.spec.ts`, `test/code-index-workflow.spec.ts`,
`test/push-webhook.spec.ts`, `test/code-index-api.spec.ts`,
`test/browser/code-index-panel.spec.tsx`.

**New database objects (migration 018):** tables `code_index_chunks`, `code_index_files`,
`code_index_state`; indexes `code_index_chunks_vector_idx`, `code_index_chunks_repo_idx`,
`code_index_files_sha_idx`.

**New exported symbols:** `buildIndexTokens`, `buildQueryTerms`, `buildQueryExpression`,
`chunkLines`, `CODE_INDEX_PATH_WEIGHT`, `CODE_INDEX_CONTENT_WEIGHT`, `CODE_INDEX_MAX_CHUNK_BYTES`,
`CODE_INDEX_MAX_FILE_BYTES`, `CODE_INDEX_MAX_QUERY_TERMS`, type `CodeIndexWindow`
(`core/code-index.ts`); `ESTIMATED_SUBREQUESTS_PER_INDEX_FILE`, `MAX_INDEX_CONTINUATIONS`,
`INDEX_FRESH_INVOCATION_YIELD_SECONDS`, `budgetAwareIndexFileLimit`, `selectIndexablePaths`,
`runIndexBuild`, types `IndexBuildParams`, `IndexBuildRunResult` (`core/code-index-build.ts`);
`upsertCodeIndexChunks`, `retrieveCodeIndexChunks`, `getCodeIndexState`,
`markCodeIndexBuildStarted`, `claimCodeIndexBuildLease`, `renewCodeIndexBuildLease`,
`releaseCodeIndexBuildLease`, `markCodeIndexBuildCompleted`, `markCodeIndexBuildFailed`,
`listIndexedPathsForSha`, `markCodeIndexFileIndexed`, `deleteCodeIndexChunksForPaths`,
`truncateCodeIndexForRepo`, types `CodeIndexChunkInput`, `CodeIndexChunkHit`, `CodeIndexStateRow`
(`db/code-index.ts`); class `IndexWorkflow`; `findRepositoryIdByIdentity` (`db/repositories.ts`);
`scorePath` (`core/priority.ts`); `isGeneratedContent` (`core/diff.ts`);
`VcsProvider.listDefaultBranchTree` and type `VcsTreeListing` (`vcs/types.ts`);
`GitHubClient.getTree`, `GitHubClient.getRepositoryMetadata`; `BitbucketClient.listSrcTree`,
`BitbucketClient.getRepositoryMetadata`, `BitbucketClient.MAX_SRC_TREE_PAGES`;
`QA_MAX_INDEX_CHARS`, `UNTRUSTED_INDEX_BEGIN`, `UNTRUSTED_INDEX_END`,
`QA_SYSTEM_PROMPT_WITH_INDEX`, type `QaIndexChunk` (`prompts/qa.ts`); `'push'` in
`supportedGitHubWebhookEvents` and type `PushWebhookPayload` (`shared/github.ts`);
`repoPushPayloadSchema` and type `RepoPushPayload` (`shared/bitbucket.ts`); config keys
`review.interactive.qa.index.{enabled,max_files,chunk_lines,top_k}` (`shared/schema.ts`);
`AppBindings.INDEX_WORKFLOW` (`server/env.ts`); the `codra-index-workflow` /
`INDEX_WORKFLOW` / `IndexWorkflow` entry in `wrangler.jsonc`; `buildCodeIndex` and
`getCodeIndexStatus` (`client/lib/api.ts`); routes
`POST /api/repos/:owner/:repo/code-index/build` and
`GET /api/repos/:owner/:repo/code-index/status`; the `INDEX_WORKFLOW` mock in `createTestEnv()`;
`MAX_INDEX_FILES_PER_INVOCATION` and `codeIndexInstanceId` (`core/code-index-build.ts`).

## Review Feedback Disposition (29-REVIEWS.md)

| Finding | Reviewer | Disposition | Where |
|---------|----------|-------------|-------|
| `make_interval(secs => $n)` portability on Postgres 17.10; rewrite to `now() + ($n \|\| ' seconds')::interval` | OpenCode 29-04 MEDIUM (#14) | **REJECTED — verified during review, no change** | Listed under REVIEWS.md "Resolved During This Review": executed against the live test database, `PostgreSQL 17.10`, `select now() + make_interval(secs => 90)` succeeds. Task 2's action keeps `make_interval` and now says so explicitly, so an executor does not "port" a working call into string concatenation — which would be strictly worse, since `make_interval` takes the seconds as a typed parameter while the concatenated form builds an interval literal out of a value. |
| `releaseCodeIndexBuildLease` clears the lease but leaves `status = 'building'`, so a cancelled build looks live until the lease expires | OpenCode 29-04 MEDIUM (#11) | **INCORPORATED** | Task 2's action now has release resolve the status too: when the row is still `building` at release time, release sets `status = 'idle'`, because a released-but-`building` row is exactly the state an operator reads as a live build. Terminal transitions still own their own status (`markCodeIndexBuildCompleted` → `ready`, `markCodeIndexBuildFailed` → `failed`) and release must not overwrite those. Pinned by a new `<behavior>` bullet and spec case. |
| `truncateCodeIndexForRepo` should report how many rows it removed so operators can observe index resets | Antigravity S-03 | **INCORPORATED** | Task 1's action now returns the deleted chunk and file counts from inside the transaction. Cheap (the delete already knows), and it gives the build a real number to log instead of "reset done". |
| A branch-creation full rebuild may fail to clear old chunks if the lease claim fails silently | Antigravity C-02 MEDIUM | **INCORPORATED, split across 29-04 and 29-05** | Here: Task 1's action states that `truncateCodeIndexForRepo` and `deleteCodeIndexChunksForPaths` are lease-agnostic primitives and that ordering them under a held lease is the caller's contract. In 29-05: the truncate is required to run only after a successful claim, and a failed claim must log the coalesced outcome rather than returning silently. |
| `code_index_files` is keyed `(repository_id, path)` so `listIndexedPathsForSha` at an *older* sha returns empty — you cannot audit what was indexed at a previous commit | OpenCode 29-04 LOW | **ACCEPTED as correct for D-12, no change** | Only the in-progress sha matters for resumability, and D-12 makes the table current-state by design. Recorded so the "missing history" reading is not mistaken for a defect. |
| Two-repository seeding for the isolation test | OpenCode 29-04 (strength) | **ALREADY PRESENT** | No change. |

<tasks>

<task type="auto">
  <name>Task 1: Refresh, full rebuild and per-file progress accessors</name>

  <reversibility rating="costly">The mutable current-state row shape and the delete-and-reinsert
  refresh contract are what every stored row depends on; moving to ref-tagged append-only rows later
  needs a migration plus a reaper (D-12).</reversibility>

  <read_first>
    - `src/server/db/code-index.ts` — as written by plan 29-01: the module header's parameterization pledge, the `env: Pick<AppBindings, 'HYPERDRIVE'>` narrowing, the positional-placeholder insert loop, and the note that the lease and refresh accessors land in this plan.
    - `src/server/db/skipped-files.ts` — the multi-row positional-placeholder pattern to reuse for the batch path delete and the per-file progress upsert.
    - `src/server/db/client.ts` — `queryTransaction` and its callback contract.
    - `src/server/db/repo-configs.ts` — an existing `queryTransaction` caller, for the shape.
    - `db/migrations/018_code_index.sql` — the three tables' exact column names and keys.
    - `.planning/phases/29-qa-idx-01-codebase-index-backed-q-a/29-RESEARCH.md` — Pattern 4's measured delete-by-path plan (an index scan on the composite primary key) and Open Question 2's recommended `code_index_state` column set.
  </read_first>

  <files>src/server/db/code-index.ts, test/code-index-db.spec.ts</files>

  <behavior>
    This task creates `test/code-index-db.spec.ts` and lands the first three case groups in it, so the
    four accessors it adds are covered by a command whose exit code reflects the result rather than by
    typecheck alone:
    - Incremental refresh: after storing chunks for two paths and then deleting one path, no `code_index_chunks` row and no `code_index_files` row remains for the deleted path, while the other path's rows are intact.
    - Full rebuild: `truncateCodeIndexForRepo` for one repository leaves a second repository's chunk count unchanged and resets only the first repository's state counters, and returns deleted chunk and file counts matching what was seeded.
    - Resumability: after `markCodeIndexFileIndexed` for two paths at a build sha, `listIndexedPathsForSha` returns exactly those two and returns an empty array for a different sha; a file recorded with a skip reason and zero chunks is still returned.
  </behavior>

  <action>
Add four accessors to `src/server/db/code-index.ts`, each taking
`env: Pick<AppBindings, 'HYPERDRIVE'>` and a single object input, each fully parameterized with
positional placeholders as the only string construction.

`deleteCodeIndexChunksForPaths(env, { repositoryId, paths })` returns early on an empty array and
otherwise issues one delete against `code_index_chunks` matching `repository_id = $1 AND path = ANY($2)`,
plus the matching delete against `code_index_files` so a removed path leaves no progress row behind.
Comment that this is D-12's incremental-refresh half and that the measured plan for it is an index
scan on the composite primary key, which is why the primary key is ordered `(repository_id, path,
chunk_start)`.

`truncateCodeIndexForRepo(env, { repositoryId })` runs inside `queryTransaction` and, in one
transaction, deletes that repository's `code_index_chunks` rows, deletes its `code_index_files` rows,
and resets its `code_index_state` counters. Comment that D-12 requires the truncate-and-reinsert to
be atomic, which is why this is a `queryTransaction` caller and not two `queryRows` calls, and that
the delete is scoped to one `repository_id` rather than being a table-level truncate so no other
repository's index is touched.

Return `{ deletedChunks: number; deletedFiles: number }` from inside that transaction rather than
`void` (addresses review concern: Antigravity S-03) — the deletes already know their row counts, and an
operator reading Worker logs after a rebuild needs "removed 4 812 chunks across 501 files", not "reset
done". Comment that the counts are for observability only and that no caller may branch on them.

Comment on both this accessor and `deleteCodeIndexChunksForPaths` that they are deliberately
**lease-agnostic primitives**: neither checks or acquires the build lease, because the same delete is
needed by a full rebuild, a push refresh, and any future repair path. Sequencing a destructive delete
after a successful `claimCodeIndexBuildLease` is therefore the *caller's* contract, stated here so a
future caller does not assume the module protects it (addresses review concern: Antigravity C-02 — the
enforcing half lands in 29-05, which is the only caller).

`listIndexedPathsForSha(env, { repositoryId, indexedSha })` returns `string[]` of the paths recorded
in `code_index_files` at that sha, empty when none. Comment that this is D-05's resumability read —
the per-unit-progress convention the review path already uses for per-file review results — and that
it must be read at the *in-progress* build sha so a resumed build skips finished files without
skipping files that were indexed at an older commit.

`markCodeIndexFileIndexed(env, { repositoryId, path, indexedSha, chunkCount, skipReason })` upserts
one `code_index_files` row with `ON CONFLICT (repository_id, path) DO UPDATE`. Comment that a file
that produced zero chunks — empty, generated, or over the byte cap — must still be recorded, with
`skipReason` set, or a resumed build re-fetches it on every continuation forever; and that
`skipReason` is a short machine-readable token, never provider text or file content.

Then create `test/code-index-db.spec.ts` and write the three case groups in the `<behavior>` block
above — refresh, full rebuild, and resumability. Task 2 appends the lease and terminal-state groups to
this same file and Task 3 completes it with the ranking, tokenization, isolation and pathological-input
groups, so establish the file's shared scaffolding here: gate it on `hasConfiguredTestDatabaseUrl()` the
way the migration specs do, seed two distinct `repositories` rows through the existing repository
accessor so the later isolation group has real rows to work with, and delete this spec's own rows in an
`afterAll` so the never-reset test database does not accumulate. Follow the DB-backed spec convention:
every assertion re-reads state from Postgres rather than trusting the accessor's return value, because
the defect class these tests exist to catch is "the accessor returned without error but nothing
persisted".

The direct spec run in `<verify>` is valid without running the full suite first because plan 29-01,
in the preceding wave, already applied migration 018 to `TEST_DATABASE_URL` via `npm test`.
  </action>

  <verify>
    <automated>npx vitest run --project node test/code-index-db.spec.ts</automated>
    <automated>npm run typecheck</automated>
    <automated>npx vitest run --project node test/code-index-tracer.spec.ts</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run --project node test/code-index-db.spec.ts` passes and contains a named case for each of the three `<behavior>` groups, so the four new accessors are covered by an exit code rather than by typecheck alone.
    - `npm run typecheck` exits 0.
    - `test/code-index-tracer.spec.ts` still passes (no regression to the accessors written in plan 29-01).
    - The spec seeds two distinct repository rows and removes its own rows in teardown.
    - Every assertion about persisted state re-reads it from Postgres.
    - `src/server/db/code-index.ts` exports `deleteCodeIndexChunksForPaths`, `truncateCodeIndexForRepo`, `listIndexedPathsForSha` and `markCodeIndexFileIndexed`.
    - `truncateCodeIndexForRepo` uses `queryTransaction`; the other three use `queryRows`.
    - `truncateCodeIndexForRepo` returns deleted chunk and file counts, and its comment plus `deleteCodeIndexChunksForPaths`' comment both state that the accessors are lease-agnostic and that ordering a destructive delete under a held lease is the caller's contract.
    - Every new statement filters on `repository_id` bound as a parameter; no statement is a table-level truncate.
    - `deleteCodeIndexChunksForPaths` deletes from both `code_index_chunks` and `code_index_files`.
    - `markCodeIndexFileIndexed` upserts with `ON CONFLICT (repository_id, path) DO UPDATE`.
    - No value is interpolated into any SQL string; the only constructed text is positional placeholders.
  </acceptance_criteria>

  <done>The module can refresh a changed path set, rebuild one repository atomically, and record per-file progress including zero-chunk skips.</done>
</task>

<task type="auto">
  <name>Task 2: Build lease and terminal-state accessors</name>

  <read_first>
    - `src/server/db/code-index.ts` — as extended by Task 1, plus `markCodeIndexBuildStarted` from plan 29-01.
    - `src/server/db/jobs.ts` — search for the existing job lease and heartbeat statements (`claimJobLease` and the lease-expiry index) and copy the conditional-update idiom: a claim is an UPDATE whose WHERE clause encodes the precondition, and success is detected from the returned row count rather than from a prior SELECT.
    - `src/server/core/audit-redact.ts` (lines 60-100) — `redactErrorMessage`, to confirm the signature the *caller* will use; this module must not import it.
    - `db/migrations/018_code_index.sql` — the `code_index_state` lease and status columns.
    - `.planning/phases/29-qa-idx-01-codebase-index-backed-q-a/29-RESEARCH.md` — Pattern 6's closing note that instance-id dedup alone is insufficient because a dead instance's id also collides, which is why a lease column is needed in addition.
  </read_first>

  <files>src/server/db/code-index.ts, test/code-index-db.spec.ts</files>

  <behavior>
    Each bullet below is both an accessor requirement and a named case appended to
    `test/code-index-db.spec.ts`, so these five accessors are covered by a command whose exit code
    reflects the result rather than by typecheck alone:
    - `claimCodeIndexBuildLease` succeeds when there is no state row, when `status` is not `building`, or when `lease_expires_at` is in the past.
    - It fails (returns false) when `status` is `building` and `lease_expires_at` is in the future and the supplied `workflowInstanceId` differs from the stored one.
    - It succeeds when the supplied `workflowInstanceId` equals the stored one, so the owning build can re-enter after a hibernation.
    - `renewCodeIndexBuildLease` extends `lease_expires_at` only for the instance that owns it.
    - `releaseCodeIndexBuildLease` on a row still marked `building` clears the lease **and** sets `status` to `idle`, so no released row reads as a live build.
    - `releaseCodeIndexBuildLease` on a row already marked `ready` or `failed` clears the lease and leaves that status untouched, so a release after a terminal transition cannot erase the recorded outcome.
    - `markCodeIndexBuildCompleted` sets `status` to `ready`, writes `indexed_sha`, `indexed_ref`, `indexed_at`, `file_count`, `chunk_count` and `truncated`, clears the lease and `building_sha`, and clears `last_error`.
    - `markCodeIndexBuildFailed` sets `status` to `failed`, stores the supplied already-redacted message in `last_error`, and clears the lease.
  </behavior>

  <action>
Add five accessors to `src/server/db/code-index.ts`.

`claimCodeIndexBuildLease(env, { repositoryId, workflowInstanceId, leaseSeconds })` returns
`Promise<boolean>`. Implement it as a single conditional upsert whose WHERE clause encodes the whole
precondition — no state row, or `status <> 'building'`, or `lease_expires_at <= now()`, or
`workflow_instance_id = $workflowInstanceId` — setting `status = 'building'`,
`workflow_instance_id`, and `lease_expires_at = now() + make_interval(secs => $n)`, and returning a
row only when the claim succeeded. Keep `make_interval(secs => $n)` exactly as written: review asked
whether it is portable and it was verified during that review against the live test database
(`PostgreSQL 17.10`, `select now() + make_interval(secs => 90)` succeeds), so do not "port" it to
`now() + ($n || ' seconds')::interval` — that rewrite would build an interval literal out of a value
where `make_interval` takes typed seconds, which is strictly worse for no portability gain on this
stack. Detect success from the returned row count. Comment that this is
the concurrency guard for a second build start: workflow instance-id dedup alone is not sufficient
because a *dead* instance's id also collides, which is exactly why the review path sets a
fresh-instance flag, so the durable lease is the authoritative guard and the instance-id dedup is
defense in depth. Comment that re-entry by the owning instance must succeed or a hibernating build
would lock itself out.

`renewCodeIndexBuildLease(env, { repositoryId, workflowInstanceId, leaseSeconds })` extends
`lease_expires_at` only where `workflow_instance_id` matches, and returns whether it did.

`releaseCodeIndexBuildLease(env, { repositoryId, workflowInstanceId })` clears the lease and
`workflow_instance_id` only where the instance matches, **and resolves the status in the same
statement**: when the row's `status` is still `'building'` at release time it is set to `'idle'`, and
any other status is left untouched (addresses review concern: OpenCode 29-04 #11). Comment why both
halves are needed. A release that clears only the lease leaves a row reading `status = 'building'`
with no live lease, which is precisely the state an operator reads on the dashboard panel as "a build
is running" for as long as nobody rebuilds — the lease expiry guards *correctness* but nothing repairs
the *display*. And the status is only forced from `'building'`, never from `'ready'` or `'failed'`,
because the terminal accessors run before release in the happy and unhappy paths respectively and
their status is the authoritative outcome; clobbering it to `'idle'` here would erase a just-recorded
success or failure.

`markCodeIndexBuildCompleted(env, { repositoryId, indexedRef, indexedSha, fileCount, chunkCount, truncated })`
sets `status = 'ready'`, writes the identity and count columns, sets `indexed_at = now()`, clears
`building_sha`, `lease_expires_at`, `workflow_instance_id`, `continuation_count` and `last_error`.
Comment that `indexed_sha` is advanced only here, on success, so an interrupted build never leaves
the row claiming a commit it did not finish.

`markCodeIndexBuildFailed(env, { repositoryId, message })` sets `status = 'failed'`, writes `message`
into `last_error`, and clears the lease. Its doc comment must state that `message` is expected to
arrive **already redacted** and that the caller routes it through `redactErrorMessage` (AUD-01),
because `db/` modules must not import `core/` — the codebase's dependency direction is routes to
core to services to models and db.

Update the module header to remove the note that lease and refresh accessors land in a later plan,
and state instead that the surface is complete for the build and retrieval consumers.

Then append the lease and terminal-state case groups to `test/code-index-db.spec.ts`, reusing the
scaffolding and teardown Task 1 established rather than adding a second seeding path. The lease group
must exercise all four transitions — first claim succeeds, a foreign claim while the lease is live
fails, a same-instance re-claim succeeds so a hibernating build cannot lock itself out, and a foreign
claim after release succeeds — plus the two release-status cases: release from `building` lands on
`idle`, and release after a terminal transition leaves `ready` or `failed` intact. The terminal-state
group asserts that `markCodeIndexBuildCompleted` advances `indexed_sha` and clears the lease, and that
`markCodeIndexBuildFailed` stores the supplied message and clears the lease, both re-read from
Postgres.
  </action>

  <verify>
    <automated>npx vitest run --project node test/code-index-db.spec.ts</automated>
    <automated>npm run typecheck</automated>
    <automated>npx vitest run --project node test/code-index-tracer.spec.ts</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run --project node test/code-index-db.spec.ts` passes and now contains a named case for each of the eight `<behavior>` bullets in addition to Task 1's three groups.
    - The lease group exercises all four transitions: first claim true, foreign claim false, same-instance re-claim true, post-release foreign claim true.
    - The lease group additionally asserts that release from `building` sets `status` to `idle`, and that release after `markCodeIndexBuildCompleted` or `markCodeIndexBuildFailed` leaves that terminal status intact — both re-read from Postgres.
    - `claimCodeIndexBuildLease` still uses `make_interval(secs => $n)`; the portability rewrite was rejected on a live-database verification and must not be applied.
    - `npm run typecheck` exits 0.
    - `src/server/db/code-index.ts` exports `claimCodeIndexBuildLease`, `renewCodeIndexBuildLease`, `releaseCodeIndexBuildLease`, `markCodeIndexBuildCompleted` and `markCodeIndexBuildFailed`.
    - `claimCodeIndexBuildLease` returns `Promise<boolean>` and decides from the returned row count of a single conditional statement, not from a separate read-then-write.
    - `src/server/db/code-index.ts` contains no import from `@server/core/` or a relative `../core/` path.
    - `markCodeIndexBuildFailed`'s doc comment states that the caller supplies an already-redacted message and names `redactErrorMessage` as the caller's responsibility.
    - `markCodeIndexBuildCompleted` is the only accessor that writes `indexed_sha` to a completed value.
    - The module header no longer claims the accessor surface is incomplete.
  </acceptance_criteria>

  <done>A build can claim, hold, renew and release an exclusive lease that survives hibernation, and terminal state records success or an already-redacted failure.</done>
</task>

<task type="auto">
  <name>Task 3: Complete the spec with the ranking, tokenization, isolation and pathological-input guarantees</name>

  <read_first>
    - `src/server/db/code-index.ts` — the complete accessor surface after Tasks 1 and 2.
    - `src/server/core/code-index.ts` — `buildIndexTokens`, `buildQueryTerms`, `buildQueryExpression`, `chunkLines`, `CODE_INDEX_MAX_CHUNK_BYTES`.
    - `test/code-index-db.spec.ts` — as created in Task 1 and extended in Task 2: its gating, its two-repository seeding, its teardown and its existing five case groups. Reuse that scaffolding; do not add a second seeding path.
    - `test/code-index-tracer.spec.ts` — the seeding and cleanup approach established in plan 29-01, which Task 1's scaffolding follows.
    - `test/learned-rules-api.spec.ts` (lines 1-60) — the DB-backed spec header convention: state what pure unit tests structurally cannot cover, and the invariant that every assertion re-reads from Postgres rather than trusting a return value.
    - `test/helpers.ts` — `createTestEnv`, `hasConfiguredTestDatabaseUrl`.
    - `.planning/phases/29-qa-idx-01-codebase-index-backed-q-a/29-VALIDATION.md` — the six `test/code-index-db.spec.ts` rows in the per-task verification map, and the test-environment hazards section.
    - `.planning/phases/29-qa-idx-01-codebase-index-backed-q-a/29-RESEARCH.md` — Pattern 1 (the measured raw-path tokenization failure), Pattern 2 (the measured 10x path-versus-body margin), Pitfall 6 (the tsvector byte ceiling and the silently-dropped oversize lexeme).
  </read_first>

  <files>test/code-index-db.spec.ts</files>

  <behavior>
    Tasks 1 and 2 already landed the refresh, full-rebuild, resumability, lease and terminal-state
    groups in `test/code-index-db.spec.ts`. This task completes the file with the four retrieval-side
    guarantees, which are the ones no earlier task could cover because they depend on the whole
    write-then-rank path:
    - Ranking: two chunks stored for one repository, one whose path contains the query word and one that mentions it only in its body; retrieval returns the path-match first. This is the D-04 guarantee.
    - Path tokenization: a query for a single word that appears only inside a stored chunk's slash-separated path returns that chunk. This case fails if raw paths are ever fed to the vector, which is the regression this test exists to catch.
    - Cross-repository isolation: two seeded repositories both hold chunks matching the same query; a retrieval for the first returns only the first repository's rows, asserted by re-reading the returned paths. This is the assertion that mitigates the high-severity information-disclosure threat in this plan's register.
    - Pathological input: a chunk whose content exceeds `CODE_INDEX_MAX_CHUNK_BYTES` and a chunk consisting of one very long unbroken token both insert successfully after passing through the JS cap, and the insert does not raise.
  </behavior>

  <action>
Append the four `<behavior>` case groups to `test/code-index-db.spec.ts`, reusing the gating, the
two-repository seeding and the `afterAll` teardown Task 1 established rather than adding a second
scaffolding path. The second seeded repository already exists for exactly the isolation case here.

Follow the DB-backed spec convention the file already uses: every assertion re-reads the state from
Postgres rather than trusting the accessor's return value, because the defect class these tests exist
to catch is "the accessor returned without error but nothing persisted". For the isolation case,
assert on the set of returned paths, not just on the row count — a count alone cannot distinguish
"returned the right repository's two rows" from "returned one row from each".

Each case's name states the guarantee. Extend the file header to state the three measured Postgres
facts these tests guard: that a raw slash path tokenizes to one unsearchable lexeme so the path vector
must be fed pre-split tokens; that the A and D weight labels produce the path-over-body ordering with
no post-processing; and that `to_tsvector` raises above roughly 1 MB of resulting vector, which is why
the JS byte cap must run before the insert.

Also record the environment hazards in that header so a future reader does not misdiagnose them: the
live test Postgres answers on port 5433 while `.env.test` names 5432, so a connection refusal here is
an environment problem rather than a code problem; and the test database is never reset, so row
accumulation is fixed by truncating, never by changing production code.
  </action>

  <verify>
    <automated>npx vitest run --project node test/code-index-db.spec.ts</automated>
    <automated>set -o pipefail; npm test 2>&1 | tail -30</automated>
    <automated>npm run typecheck</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run --project node test/code-index-db.spec.ts` passes and its exit code is the spec's; this is the command that proves the ranking guarantee and the cross-repository isolation guarantee, so neither may be reachable only through a piped invocation.
    - `set -o pipefail; npm test 2>&1 | tail -30` passes and its exit code is the suite's, not `tail`'s.
    - `npm run typecheck` exits 0.
    - The completed spec contains a named case for each of this task's four `<behavior>` bullets, in addition to the five groups Tasks 1 and 2 landed.
    - The isolation case asserts the returned path set, not merely a count.
    - The path-tokenization case queries a word that appears only in the path, not in the content.
    - The pathological case asserts the insert resolves rather than asserting an error is thrown.
    - The spec still removes its own rows in teardown.
  </acceptance_criteria>

  <done>`test/code-index-db.spec.ts` is complete: ranking, path tokenization, cross-repository isolation, refresh, full rebuild, resumability, the concurrency lease, terminal state and pathological input are each pinned by a named case running against the live test Postgres, reachable by a direct unpiped command.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| one repository's chunk rows → another repository's retrieval | A single table holds every tenant's indexed source |
| build failure text → `code_index_state.last_error` → operator surface | Provider and runtime error text becomes operator-visible state |
| concurrent build starts → one `code_index_state` row | Two triggers (dashboard press, push event) can race |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-04-01 | Information Disclosure | `retrieveCodeIndexChunks` and every accessor in `src/server/db/code-index.ts` | high | mitigate | Every statement filters on a bound `repository_id`; the full rebuild deletes by `repository_id` rather than truncating the table. Pinned by the two-repository isolation case and the cross-repository rebuild case in `test/code-index-db.spec.ts`. |
| T-29-04-02 | Information Disclosure | `markCodeIndexBuildFailed` writing `last_error` | high | mitigate | The accessor's contract states the message arrives already redacted and names `redactErrorMessage` as the caller's responsibility (AUD-01); the module imports nothing from `core/`, so redaction cannot be silently skipped by this layer taking it over. |
| T-29-04-03 | Tampering | all new statements | high | mitigate | Fully parameterized through `queryRows` / `queryTransaction`; the only constructed SQL text is positional placeholders, never a value. Asserted as a source criterion. |
| T-29-04-04 | Denial of Service | `claimCodeIndexBuildLease` | medium | mitigate | The lease is a single conditional statement with an expiry, so a repeated build trigger cannot start a second concurrent build and a crashed build cannot hold the lease forever. Four-transition lease case pins it. |
| T-29-04-05 | Denial of Service | `upsertCodeIndexChunks` with pathological content | high | mitigate | The JS byte cap from `core/code-index.ts` runs before the insert, because `to_tsvector` raises above roughly 1 MB of resulting vector and that raise would fail the build step and burn its retry budget. Pinned by the pathological-input case. |
| T-29-04-06 | Repudiation | build lifecycle state | low | accept | No audit events are emitted for index builds; the `code_index_state` `status` plus already-redacted `last_error` is the operator surface. Deliberate (D-15's reasoning, and the phase excludes index events from the job audit-trail viewer since a Q&A is not a job). |
| T-29-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package is installed by this plan; the zero-new-deps standing decision holds and RESEARCH.md's Package Legitimacy Audit table is empty by construction. |
</threat_model>

<verification>
- `npm test` green with the new DB spec included.
- `npm run typecheck` clean.
- Every guarantee in the `<behavior>` blocks has a named spec case.
</verification>

<success_criteria>
- The DB module's accessor surface is complete for the build Workflow and the retrieval path.
- Cross-repository isolation, refresh-without-orphans, resumability, the concurrency lease and pathological-input tolerance are each proven against the live test Postgres.
- `last_error` redaction responsibility is stated at the boundary and the module imports nothing from `core/`.
</success_criteria>

<output>
Create `.planning/phases/29-qa-idx-01-codebase-index-backed-q-a/29-04-SUMMARY.md` when done.
</output>
