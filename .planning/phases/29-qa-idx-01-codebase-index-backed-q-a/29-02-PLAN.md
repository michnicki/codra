---
phase: 29-qa-idx-01-codebase-index-backed-q-a
plan: 02
type: execute
wave: 2
depends_on:
  - 29-01
files_modified:
  - src/shared/schema.ts
  - test/repo-configs.spec.ts
autonomous: false
requirements:
  - QA-IDX-01
must_haves:
  truths:
    - "D-16: config is expressed as an `index` block nested under the existing `review.interactive.qa` path, carrying `enabled` (default false), `max_files`, `chunk_lines` and `top_k`, each numeric key bounded by `.max()`."
    - "The `max_files` bound is the developer-confirmed ceiling (2 000 unless changed at the checkpoint), pinned by a boundary spec pair rather than asserted in a comment, and its schema comment states the 12-files-per-invocation build-rate derivation so the ceiling's wall-clock meaning is visible where the value is chosen."
    - "NREG-01: `repoConfigSchema.parse({})` yields `review.interactive.qa.index.enabled === false` AND every sibling default present as a real value, not undefined."
    - "The new key is added at all three Zod sites — the inner `z.object`, that block's own `.default({...})` literal, and the outer `reviewConfigSchema.default({...})` literal — because Zod 4 returns `.default(literal)` values without re-parsing them."
    - "The pinned default-baseline oracle still matches after the config addition, so a repository that has not opted in behaves exactly as before."
    - "`normalizeRepoConfig` needs no change: it is spread-based, so the new key passes through unmodified."
  prohibitions:
    - statement: "MUST NOT make the new index configuration reachable by default — a repository whose operator has not explicitly opted in must see no change in Q&A behavior, no index storage, and no build activity."
      category: values
  artifacts:
    - path: src/shared/schema.ts
      provides: "review.interactive.qa.index config block with bounded numeric knobs"
      contains: "index: z"
    - path: test/repo-configs.spec.ts
      provides: "NREG-01 assertions for the new block at all three Zod sites"
  key_links:
    - from: src/shared/schema.ts
      to: src/server/db/repo-configs.ts
      via: "parsed_json round-trip through parseJsonColumn / normalizeRepoConfig (spread-based, no change needed)"
      pattern: "normalizeRepoConfig"
---

<objective>
Add the `review.interactive.qa.index` configuration block that gates every other part of this phase, and prove the default-off contract holds at all three Zod sites.

Purpose: this is the NREG-01 gate. Every downstream plan — the Q&A retrieval call, both push-webhook branches, the build endpoint — reads this toggle before doing anything. It is also a one-way door: once a key lands in `repo_configs.parsed_json` for a repository that set it, removing the key risks breaking config parsing for that repository, so the key names and shape are confirmed with the developer before they are written.

Output: the nested `index` block at three Zod sites in `src/shared/schema.ts`, plus assertions in `test/repo-configs.spec.ts` that `repoConfigSchema.parse({})` yields the toggle false with every sibling default materialized.
</objective>

<execution_context>
@/home/thomas/repos-ext/codra/.claude/gsd-core/workflows/execute-plan.md
@/home/thomas/repos-ext/codra/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-qa-idx-01-codebase-index-backed-q-a/29-CONTEXT.md
@.planning/phases/29-qa-idx-01-codebase-index-backed-q-a/29-RESEARCH.md
@.planning/phases/29-qa-idx-01-codebase-index-backed-q-a/29-PATTERNS.md

<interfaces>
The three edit sites, located in the current file:

`src/shared/schema.ts:217-224` — the inner `qa` `z.object`, today:
  qa: z.object({ enabled: boolean default false, rate_limit_per_hour: int positive default 10 })
       .default({ enabled: false, rate_limit_per_hour: 10 })          <- site 2 is this literal

`src/shared/schema.ts:228` — inside `interactive`'s own `.default({...})` literal:
  qa: { enabled: false, rate_limit_per_hour: 10 }

`src/shared/schema.ts:337` — inside the outer `reviewConfigSchema.default({...})` literal:
  interactive: { commands: {...}, qa: { enabled: false, rate_limit_per_hour: 10 } }

The Phase 28 `learning` block at `src/shared/schema.ts:284-300` and `:346` is the worked
three-site precedent, including the in-file comment that names this exact Zod trap.
</interfaces>
</context>

## Artifacts this phase produces

Phase 29 creates the following; treat every symbol as new rather than as an unresolved reference.

**New files:** `db/migrations/018_code_index.sql`, `src/server/core/code-index.ts`,
`src/server/core/code-index-build.ts`, `src/server/db/code-index.ts`,
`src/server/workflows/index-build.ts`,
`src/client/components/features/repos/code-index-panel.tsx`, and the specs
`test/code-index-tracer.spec.ts`, `test/migration-018-idempotency.spec.ts`,
`test/code-index-splitter.spec.ts`, `test/code-index-chunker.spec.ts`,
`test/code-index-query.spec.ts`, `test/code-index-db.spec.ts`, `test/code-index-tree.spec.ts`,
`test/code-index-workflow.spec.ts`, `test/push-webhook.spec.ts`, `test/code-index-api.spec.ts`,
`test/browser/code-index-panel.spec.tsx`.

**New database objects (migration 018):** tables `code_index_chunks`, `code_index_files`,
`code_index_state`; indexes `code_index_chunks_vector_idx`, `code_index_chunks_repo_idx`,
`code_index_files_sha_idx`.

**New exported symbols:** `buildIndexTokens`, `buildQueryTerms`, `buildQueryExpression`,
`chunkLines`, `CODE_INDEX_PATH_WEIGHT`, `CODE_INDEX_CONTENT_WEIGHT`, `CODE_INDEX_MAX_CHUNK_BYTES`,
`CODE_INDEX_MAX_FILE_BYTES`, `CODE_INDEX_MAX_QUERY_TERMS`, type `CodeIndexWindow`
(`core/code-index.ts`); `ESTIMATED_SUBREQUESTS_PER_INDEX_FILE`, `MAX_INDEX_CONTINUATIONS`,
`INDEX_FRESH_INVOCATION_YIELD_SECONDS`, `budgetAwareIndexFileLimit`, `selectIndexablePaths`,
`runIndexBuild`, types `IndexBuildParams`, `IndexBuildRunResult` (`core/code-index-build.ts`);
`upsertCodeIndexChunks`, `retrieveCodeIndexChunks`, `getCodeIndexState`,
`markCodeIndexBuildStarted`, `claimCodeIndexBuildLease`, `renewCodeIndexBuildLease`,
`releaseCodeIndexBuildLease`, `markCodeIndexBuildCompleted`, `markCodeIndexBuildFailed`,
`listIndexedPathsForSha`, `markCodeIndexFileIndexed`, `deleteCodeIndexChunksForPaths`,
`truncateCodeIndexForRepo`, types `CodeIndexChunkInput`, `CodeIndexChunkHit`, `CodeIndexStateRow`
(`db/code-index.ts`); class `IndexWorkflow`; `findRepositoryIdByIdentity` (`db/repositories.ts`);
`scorePath` (`core/priority.ts`); `isGeneratedContent` (`core/diff.ts`);
`VcsProvider.listDefaultBranchTree` and type `VcsTreeListing` (`vcs/types.ts`);
`GitHubClient.getTree`, `GitHubClient.getRepositoryMetadata`; `BitbucketClient.listSrcTree`,
`BitbucketClient.getRepositoryMetadata`, `BitbucketClient.MAX_SRC_TREE_PAGES`;
`QA_MAX_INDEX_CHARS`, `UNTRUSTED_INDEX_BEGIN`, `UNTRUSTED_INDEX_END`,
`QA_SYSTEM_PROMPT_WITH_INDEX`, type `QaIndexChunk` (`prompts/qa.ts`); `'push'` in
`supportedGitHubWebhookEvents` and type `PushWebhookPayload` (`shared/github.ts`);
`repoPushPayloadSchema` and type `RepoPushPayload` (`shared/bitbucket.ts`); config keys
`review.interactive.qa.index.{enabled,max_files,chunk_lines,top_k}` (`shared/schema.ts`);
`AppBindings.INDEX_WORKFLOW` (`server/env.ts`); the `codra-index-workflow` /
`INDEX_WORKFLOW` / `IndexWorkflow` entry in `wrangler.jsonc`; `buildCodeIndex` and
`getCodeIndexStatus` (`client/lib/api.ts`); routes
`POST /api/repos/:owner/:repo/code-index/build` and
`GET /api/repos/:owner/:repo/code-index/status`; the `INDEX_WORKFLOW` mock in `createTestEnv()`;
`MAX_INDEX_FILES_PER_INVOCATION` and `codeIndexInstanceId` (`core/code-index-build.ts`).

## Review Feedback Disposition (29-REVIEWS.md)

| Finding | Reviewer | Disposition | Where |
|---------|----------|-------------|-------|
| Task 1's config-shape decision is a **BLOCKING** checkpoint that must be resolved before Wave 2 implementation | OpenCode 29-02 HIGH (BLOCKING), Consensus Agreed Concern 3 | **PRESERVED — still blocking** | Task 1 remains `checkpoint:decision` with `gate="blocking"` and `autonomous: false` on the plan. Both reviewers noted no reviewer can resolve it. Task 2 must not write a byte of `src/shared/schema.ts` before the selection is recorded. |
| `.max(5_000)` on `max_files` is far looser than the continuation budget can service; either tighten it or document the effective cap | OpenCode 29-02 MEDIUM (#8), Consensus Agreed Concern 4 (recorded as "arithmetically sound") | **INCORPORATED — bound tightened to 2 000 and the arithmetic documented** | Task 1's context now carries the derived numbers as a third thing the developer confirms, and Task 2's action requires the arithmetic in the schema comment plus a rejection spec case at the new bound. The reviewers' framing of "silently-capped build" is corrected in the plan: the build is not capped, it takes proportionally longer, and that is the fact an operator needs stated. |
| The comment says `max_files` caps fetches not stored files, but the schema cannot express that semantic | OpenCode 29-02 LOW | **ACCEPTED as a comment-only contract, no change** | Correct — it is a consequence of content-based generated detection (D-09) and belongs in prose. Task 2's action already requires that per-key note; recorded here so it is not mistaken for an omission. |
| `chunk_lines` default 50 matches `WINDOW_LINE_COUNT`, but changing it needs a full re-index | OpenCode 29-02 suggestion 3 | **ALREADY PRESENT** | Task 2's action already requires that per-key note (D-10). No change. |

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm the config key names and defaults before they enter parsed_json</name>
  <decision>The four configuration keys and their default values, and the config path they live at.</decision>
  <context>
Adding a key to `repo_configs.parsed_json` is a one-way door (D-16 self-rated **one-way**): once a
repository's stored config contains the key, removing it later risks breaking config parsing for
that repository. The key names also become the operator-facing vocabulary in the dashboard panel
and any documentation.

Two facts shape the proposal. First, REQUIREMENTS names the toggle `qa.index_enabled`, but the
existing Q&A config already lives at `review.interactive.qa` — a new top-level `qa` block would
split Q&A configuration across two places. Second, the discussion added three knobs beyond the
toggle (`index_max_files`, chunk line size, top-K), which are naturally siblings of the toggle
rather than four flat keys.

Default-off means every existing repository starts at the proposed defaults regardless of which
option is chosen, so this is a naming and shape decision, not a migration decision.

**Third thing to confirm while the developer is here — the `max_files` upper bound and what it costs.**
Cross-AI review flagged that the originally-proposed `.max(5_000)` is far looser than the build's
own arithmetic can service in reasonable wall-clock time. The derivation, from constants that
already exist in this repository: `TokenTracker` has `MAX_SUBREQUESTS = 50` and `SAFE_MARGIN = 25`,
so a fresh invocation's `remainingSafeBudget()` is **25**; at
`ESTIMATED_SUBREQUESTS_PER_INDEX_FILE = 2` that funds `floor(25 / 2) = 12` files per invocation; and
each continuation sleeps `INDEX_FRESH_INVOCATION_YIELD_SECONDS = 60` to force hibernation. So a build
advances roughly **12 files per minute**: 500 files is about 42 minutes, 2 000 files about 2.8 hours,
5 000 files about 7 hours.

Correct the reviewers' framing when presenting this: the build is **not** silently capped at some
lower number — `MAX_INDEX_CONTINUATIONS` hands off to a fresh instance rather than failing, so a
5 000-file build does finish. It simply takes hours of wall clock and provider quota. The proposal is
therefore `.max(2_000)` rather than `.max(5_000)`: it keeps a generous 4x headroom over the 500
default while bounding the worst legal build an operator can request to roughly three hours. Ask the
developer to confirm `2_000`, or to name a different ceiling knowing the per-minute rate above.
  </context>
  <options>
    <option id="nested-index-block">
      <name>Nested block: `review.interactive.qa.index.{enabled, max_files, chunk_lines, top_k}` with defaults `false / 500 / 50 / 8`</name>
      <pros>Keeps all Q&amp;A configuration under one path; the toggle reads as `qa.index.enabled` which is REQUIREMENTS' `qa.index_enabled` expressed at the existing path; groups the three knobs with the toggle they belong to; one `.max()`-bounded numeric block matching the Phase 13 bounds precedent.</pros>
      <cons>Four levels of nesting (`review.interactive.qa.index.enabled`); needs the key added at three Zod sites rather than two.</cons>
    </option>
    <option id="flat-qa-keys">
      <name>Flat keys on the existing `qa` object: `index_enabled`, `index_max_files`, `index_chunk_lines`, `index_top_k`</name>
      <pros>Shallower; `index_enabled` matches REQUIREMENTS' spelling literally; only two Zod sites to edit for the sibling literal.</pros>
      <cons>Four prefixed keys instead of one grouped block; no natural home for a future fifth knob; diverges from the Phase 28 `learning` block shape that groups a feature's keys together.</cons>
    </option>
  </options>
  <action>Present the two options above with their trade-offs, then present the `max_files` upper-bound question with the per-invocation arithmetic from the context block (12 files per minute; 500 files ≈ 42 min, 2 000 ≈ 2.8 h, 5 000 ≈ 7 h) and the correction that a large value is slow rather than silently capped. Wait for the developer's selection on both, and record the chosen key path, the four default values and the confirmed `max_files` ceiling in the plan summary so Task 2 implements exactly what was chosen. Do not write any file before the selection is given.</action>
  <resume-signal>Select: nested-index-block, flat-qa-keys, or describe a different shape — and confirm the max_files ceiling (proposed 2000)</resume-signal>
  <done>The developer has selected a config shape and confirmed the `max_files` ceiling, and the chosen key path plus the four default values plus that ceiling are recorded for Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Add the index config block at all three Zod sites and pin the default-off contract</name>

  <reversibility rating="one-way">The config keys enter `repo_configs.parsed_json` for any
  repository that sets them; removing a key after it ships risks breaking config parsing for those
  repositories (D-16).</reversibility>

  <read_first>
    - `src/shared/schema.ts` — the whole `reviewConfigSchema`, specifically the `interactive` block around lines 201-230, the Phase 28 `learning` block around lines 279-300, and the outer `.default({...})` literal around lines 325-347 including the in-file comment that names the Zod default short-circuit trap.
    - `src/server/db/repo-configs.ts` — `parseJsonColumn`, `normalizeRepoConfig` and `upsertRepoConfig`; confirm the normalizer is spread-based so the new key needs no change there.
    - `test/repo-configs.spec.ts` — the existing default-config assertions and their style.
    - `test/phase19-default-bytes-identical.spec.ts` and `test/phase19-default-baseline.fixture.json` — the pinned default oracle this change must not disturb.
    - `.planning/phases/29-qa-idx-01-codebase-index-backed-q-a/29-RESEARCH.md` — Pitfall 5, with the measured four-case Zod characterization showing the key is absent when only the inner object is edited.
  </read_first>

  <files>src/shared/schema.ts, test/repo-configs.spec.ts</files>

  <behavior>
    - `repoConfigSchema.parse({})` yields `review.interactive.qa.index.enabled === false`.
    - The same parse yields `max_files === 500`, `chunk_lines === 50`, `top_k === 8` as real numbers, not `undefined`.
    - Parsing a config that supplies `review.interactive.qa` as `{}` still yields all four index defaults.
    - Parsing a config that supplies `review.interactive.qa.index.enabled = true` and nothing else yields the other three at their defaults.
    - A value above the declared `.max()` bound for any numeric key is rejected by the schema.
    - `max_files` accepts the confirmed ceiling (`2_000` unless the developer chose otherwise) and rejects that ceiling plus one, so the tightened bound is pinned rather than asserted in a comment.
    - `defaultRepoConfig.review.interactive.qa.rate_limit_per_hour` still equals 10 and `enabled` still equals false.
  </behavior>

  <action>
Implement the option the developer selected in Task 1. The action below is written for
`nested-index-block`; if `flat-qa-keys` was selected, apply the same three-site discipline and the
same bounds to the four flat keys instead, and keep the rest of this action's requirements intact.

Site 1 — the inner `qa` `z.object` in `src/shared/schema.ts`: add an `index` property that is a
`z.object` with `enabled: z.boolean().default(false)`,
`max_files: z.number().int().positive().max(2_000).default(500)`,
`chunk_lines: z.number().int().positive().max(500).default(50)`, and
`top_k: z.number().int().positive().max(50).default(8)`, and give that object its own
`.default({ enabled: false, max_files: 500, chunk_lines: 50, top_k: 8 })`. Use whatever `max_files`
ceiling the developer confirmed in Task 1 if it differs from `2_000`. Precede it with a comment
in the style of the Phase 28 `learning` block: tag QA-IDX-01 and D-16, state that `enabled` is
REQUIREMENTS' `qa.index_enabled` expressed at the pre-existing `review.interactive.qa` config path,
state the NREG-01 inertness claim in the form "`repoConfigSchema.parse({})` yields
`review.interactive.qa.index.enabled === false`", and state that the `.max()` bounds exist because a
bound guards against an authenticated-but-malicious config write (the Phase 13 bounds precedent).
Add a per-key note on the two knobs whose value is load-bearing: `max_files` caps *fetches*, not
stored files, because generated-file detection is content-based so a file must be fetched before it
can be dropped; and `chunk_lines` is baked into stored rows, so changing it requires a full re-index
rather than a migration.

`max_files` additionally needs the build-cost arithmetic in its comment, because the number an
operator types here is the only place the build's wall-clock cost is chosen (addresses review
concern: OpenCode 29-02 #8 / Consensus Agreed Concern 4). State the derivation from the constants
that already exist: `TokenTracker`'s `MAX_SUBREQUESTS = 50` minus `SAFE_MARGIN = 25` leaves a fresh
`remainingSafeBudget()` of 25, `ESTIMATED_SUBREQUESTS_PER_INDEX_FILE = 2` funds `floor(25 / 2) = 12`
files per invocation, and each continuation sleeps `INDEX_FRESH_INVOCATION_YIELD_SECONDS = 60` to
force hibernation — so a build advances roughly 12 files per minute, putting the 500 default at about
42 minutes and the `2_000` ceiling at about 2.8 hours. State explicitly that a large value is **slow,
not silently capped**: `MAX_INDEX_CONTINUATIONS` hands off to a fresh instance rather than
abandoning the build, so the ceiling exists to bound how long an operator can ask a build to run, not
to bound how much gets indexed. Do **not** add a `.refine()` cross-field validation against the
budget constants: `src/shared/schema.ts` is imported by the dashboard client, and reaching into
`core/code-index-build.ts` from it would drag server-side build constants into the browser bundle and
invert the layering (shared depends on nothing server-side). The bound plus the documented arithmetic
is the whole mitigation.

Site 2 — the `.default({...})` literal on the `interactive` block (the `qa: { enabled: false,
rate_limit_per_hour: 10 }` entry): add the same `index: { enabled: false, max_files: 500,
chunk_lines: 50, top_k: 8 }` object.

Site 3 — the outer `reviewConfigSchema.default({...})` literal's `interactive.qa` entry: add the
identical `index` object. All three literals must carry the same values; a divergence between them
is a silent config bug that only shows up for repositories whose parent key happens to be absent.

Do not touch `QA_DIFF_CHAR_DIVISOR`, any other `reviewConfigSchema` default, or
`normalizeRepoConfig`. Confirm by reading `src/server/db/repo-configs.ts` that the normalizer is
spread-based, and state that finding in the summary rather than editing it.

Extend `test/repo-configs.spec.ts` with the seven `<behavior>` assertions. Include the case where the
parent `qa` key is supplied as an empty object and the case where only `index.enabled` is supplied,
since those are the two shapes that distinguish a correct three-site edit from an inner-object-only
edit. Add a rejection case for an out-of-bounds numeric value, and a boundary pair on `max_files` that
accepts the confirmed ceiling and rejects ceiling-plus-one, so the tightened bound cannot drift back
to a looser value without failing the suite.
  </action>

  <verify>
    <automated>npx vitest run --project node test/repo-configs.spec.ts test/phase19-default-bytes-identical.spec.ts</automated>
    <automated>npm run typecheck</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run --project node test/repo-configs.spec.ts test/phase19-default-bytes-identical.spec.ts` passes.
    - `npm run typecheck` exits 0.
    - `src/shared/schema.ts` contains the four index keys at three distinct locations: inside the inner `qa` `z.object`, inside the `interactive` block's `.default({...})` literal, and inside the outer `reviewConfigSchema.default({...})` literal.
    - The three literal occurrences carry identical values (`false`, `500`, `50`, `8`).
    - `test/repo-configs.spec.ts` asserts `repoConfigSchema.parse({}).review.interactive.qa.index.enabled === false` and asserts each sibling default is a number rather than `undefined`.
    - `test/repo-configs.spec.ts` contains a case parsing `{ review: { interactive: { qa: {} } } }` and asserting all four index defaults are present.
    - `test/repo-configs.spec.ts` contains a `max_files` boundary pair: the confirmed ceiling parses, ceiling-plus-one is rejected.
    - The `max_files` comment in `src/shared/schema.ts` states the 12-files-per-invocation derivation and that a large value is slow rather than silently capped.
    - `src/shared/schema.ts` contains no `.refine()` or cross-field validation referencing a `core/` constant, and imports nothing from `@server/`.
    - `test/phase19-default-bytes-identical.spec.ts` passes without editing `test/phase19-default-baseline.fixture.json`.
    - `src/server/db/repo-configs.ts` is unmodified.
  </acceptance_criteria>

  <done>`repoConfigSchema.parse({})` yields the index toggle false with every sibling default materialized, the pinned default oracle still matches, and the three-site Zod discipline is pinned by tests that would fail on an inner-object-only edit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dashboard config write → `repo_configs.parsed_json` | An authenticated operator supplies numeric knobs that later bound provider fetches, storage growth and prompt size |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-02-01 | Denial of Service | `review.interactive.qa.index` numeric keys in `src/shared/schema.ts` | high | mitigate | Every numeric key carries a `.max()` bound (`max_files` 2 000 — tightened from 5 000 on review, `chunk_lines` 500, `top_k` 50) so an authenticated-but-malicious config write cannot request an unbounded build or an unbounded retrieval, following the Phase 13 bounds precedent. The `max_files` ceiling is sized against the build's own 12-files-per-invocation rate so the worst legal request is bounded at roughly three hours of wall clock rather than seven. Rejection and the exact boundary are asserted by spec cases. |
| T-29-02-02 | Tampering | `repoConfigSchema` default literals | high | mitigate | The default-off claim is asserted from `repoConfigSchema.parse({})` at all three Zod sites plus the pinned default-baseline oracle, so a partially-applied edit that leaves the key `undefined` at runtime fails the suite instead of silently enabling or silently breaking config parsing. |
| T-29-02-03 | Elevation of Privilege | config write path | low | accept | No new write path is introduced; the existing `PATCH /api/repos/:owner/:repo/config` route already carries `requireSession` and `requireCsrfHeader`, and this plan adds no endpoint. |
| T-29-02-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package is installed by this plan; the phase's zero-new-deps standing decision holds and RESEARCH.md's Package Legitimacy Audit table is empty by construction. |
</threat_model>

<verification>
- `npx vitest run --project node test/repo-configs.spec.ts test/phase19-default-bytes-identical.spec.ts` green.
- `npm run typecheck` clean.
- The new keys appear at three distinct Zod sites with identical values.
</verification>

<success_criteria>
- The index config block exists at the developer-confirmed path with the developer-confirmed defaults.
- Default-off is proven by parse assertions, not asserted in a comment.
- The pinned default-baseline fixture is unchanged and still matches.
</success_criteria>

<output>
Create `.planning/phases/29-qa-idx-01-codebase-index-backed-q-a/29-02-SUMMARY.md` when done.
</output>
