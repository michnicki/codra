---
phase: 26-evid-02-hard-drop-evidence-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/schema.ts
  - src/server/core/evidence.ts (NEW)
  - src/server/core/model-output.ts
  - src/server/core/config.ts
  - src/server/core/audit.ts
  - test/evidence.spec.ts (NEW)
autonomous: true
requirements:
  - EVID-02
must_haves:
  truths:
    - "drop-before-dedup: a hard-dropped finding does not participate in dedup. When a dropped finding and a kept finding have identical content, only the kept finding reaches dedup — no collision or merge ambiguity."
    - "When all findings in a file are hard-dropped, the file produces zero posted findings. The audit event still fires with droppedCount > 0 and a populated sample, so the drop is explainable even when output is empty."
    - "Hard-drop is a filter (in-memory removal before dedup), not a reorder. The relative order of surviving findings is preserved because the drop iterates the findings array and removes non-matching entries in place."
    - "The hard-drop check is deterministic: same diff haystack + same findings produce identical drop decisions. Same deterministic whitespace-collapsed substring search as EVID-01."
    - "The hard-drop is an in-memory filter in the finalize phase. Retries re-run against immutable file_reviews producing identical results. Audit event uses at-most-once gating (same pattern as `finalizeRetriedPastPost`) to prevent duplicate events on retry."
    - "Config type defaults are applied at load time: `loadRepoConfig` in config.ts re-parses DB-loaded config through `repoConfigSchema.parse()`, so a config stored before the `evidence` key existed always gets Zod defaults injected. Runtime lookups like `config.review.evidence?.hard_drop ?? false` never see `undefined`."
    - "The `hard_drop_exempt_categories` array is bounded to 20 items and uses case-insensitive category comparison so 'Security' / 'SECURITY' always match the reviewCategories enum value 'security'."
  prohibitions:
    - "The hard_drop_exempt_categories check MUST compare categories case-insensitively. A config entry 'Security' or 'SECURITY' must match the reviewCategories value 'security'. A case-sensitive comparison would silently bypass exemptions."
  artifacts:
    - schema.ts: evidence config block in reviewConfigSchema (hard_drop + hard_drop_exempt_categories with .max(20))
    - schema.ts: evidence_hard_dropped variant in jobAuditEventSchema discriminated union
    - schema.ts: inline default evidence: { hard_drop: false, hard_drop_exempt_categories: ['security'] } in repoConfigSchema
    - config.ts: loadRepoConfig re-parses DB-loaded config through repoConfigSchema.parse() to inject Zod defaults
    - core/evidence.ts (NEW): checkEvidence() function, EvidenceCheckResult interface, EvidenceDropEntry interface
    - core/audit.ts: buildEvidenceHardDroppedEvent() function
    - test/evidence.spec.ts (NEW): unit test suite covering checkEvidence, buildEvidenceHardDroppedEvent, cross-consistency
---

<objective>
Define the EVID-02 building blocks: evidence config schema (bounded), evidence_hard_dropped audit event shape, shared evidence check function (extracted from model-output.ts), audit event builder, config.ts default injection, and unit tests for all three.

Purpose: Contract-first — schema, function, and builder must exist before the hard-drop gate is wired into finalize (Plan 26-02). The extraction ensures the evidence check logic is shared between EVID-01 (parse-time soft gate) and EVID-02 (finalize-time hard gate), preventing drift. Config default injection ensures existing DB configs (predating the evidence key) always get Zod defaults.

Output: Schema definitions, shared core/evidence.ts module, config.ts DB-load fix, audit builder, unit tests.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-evid-02-hard-drop-evidence-gate/26-CONTEXT.md
@.planning/phases/26-evid-02-hard-drop-evidence-gate/26-RESEARCH.md

# Source files — read before implementing
@src/shared/schema.ts
@src/server/core/model-output.ts
@src/server/core/audit.ts
@src/server/core/config.ts
@src/server/core/review.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add evidence config block (with bounds) to reviewConfigSchema + evidence_hard_dropped audit event + config.ts default injection</name>
  <files>src/shared/schema.ts, src/server/core/config.ts</files>
  <read_first>
    src/shared/schema.ts lines 130-253 — reviewConfigSchema with all existing toggle blocks. Follow the exact pattern: z.object({ field: z.boolean().default(false), ... }).default({ ... }).
    src/shared/schema.ts lines 255-297 — repoConfigSchema inline default literal. Mirror the evidence block default there.
    src/shared/schema.ts lines 907-953 — evidence_missing_summary audit event shape. The new evidence_hard_dropped variant follows this exact pattern.
    src/shared/schema.ts line 110 — custom_rules schema with .max(50) array bound as precedent for array bounds on config.
    src/shared/schema.ts lines 1317-1322 — normalizeRepoConfig function (only normalizes model, not evidence — no change needed here).
    src/server/core/config.ts lines 78-101 — loadRepoConfig function. Need to add repoConfigSchema.parse() pass after model override (line 88) to inject Zod defaults.
    src/server/db/repo-configs.ts lines 25-40 — mapRepo function (already calls repoConfigSchema.parse() internally).
  </read_first>
  <action>
    Part A — Schema changes (schema.ts):

    Add the `evidence` config block to `reviewConfigSchema` per D-03, D-04, D-07, D-08. The array MUST be bounded at 20 items (Antigravity MEDIUM finding REVIEWS.md) for defense-in-depth to prevent config bloat — mirroring the `custom_rules` .max(50) precedent at schema.ts:110:

    ```
    evidence: z.object({
      hard_drop: z.boolean().default(false),
      hard_drop_exempt_categories: z.array(z.string()).default(['security']).max(20),
    }).default({ hard_drop: false, hard_drop_exempt_categories: ['security'] }),
    ```

    The `.max(20)` prevents operator config bloat. The default `['security']` means security findings always post regardless of evidence quality. An operator can set it to `[]` to hard-drop everything (no exemptions).

    Comment rationale: note the array bound mirrors `custom_rules` .max(50) precedent and prevents unbounded config accumulation.

    Add the `evidence_hard_dropped` variant to `jobAuditEventSchema` discriminated union. Inserted after the `evidence_missing_summary` arm (after line 953) and before the `rounds.detected` arm (around line 966). Shape per D-05/D-06:

    ```
    z.object({
      stage: z.literal('evidence_hard_dropped'),
      file: z.string(),
      pass: fileReviewPassSchema,
      droppedCount: z.number().int().min(0),
      sample: z.array(
        z.object({
          path: z.string(),
          line: z.number().nullable().optional(),
          title: z.string().max(100),
          reason: z.enum(['absent', 'not_in_hunk']),
        }),
      ).max(20),
      timestamp: dateStringSchema,
    }).passthrough(),
    ```

    Comments: note design follows `evidence_missing_summary` precedent; titles route through `redactFindingTitle` at production time (AUD-01); each entry carries a `reason` field per D-06.

    Update the `repoConfigSchema` inline default (schema.ts around line 296, inside the `review: reviewConfigSchema.default({...})` block). Add:

    ```
    evidence: { hard_drop: false, hard_drop_exempt_categories: ['security'] },
    ```

    Place alongside other toggle defaults. The default literal must match the Zod default so `repoConfigSchema.parse({})` yields `evidence.hard_drop === false`.

    Part B — Config load-time default injection (config.ts):

    In `src/server/core/config.ts`, update the `loadRepoConfig` function to ensure DB-loaded configs always get Zod defaults applied. This protects against an existing DB config that was stored before the `evidence` key existed (CRITICAL PATH FIX #1 — REVIEWS.md consensus).

    Currently at line 78-88:
    ```
    let parsedJson = existing?.parsedJson ?? defaultRepoConfig;
    ...
    if (!hasRepoModelOverride(existing)) {
      const globalModel = await getGlobalConfig(env);
      parsedJson = {
        ...parsedJson,
        model: globalModel
      };
    }
    ```

    After the model override block (line 88, before the `finalConfig` construction at line 90), add:
    ```
    // REVIEWS CRITICAL FIX #1: re-parse through repoConfigSchema to inject Zod defaults for any keys
    // added after this config was stored (e.g., the evidence block from EVID-02). Without this, old
    // DB configs would have undefined for new keys and runtime lookups like `?? false` would no-op.
    parsedJson = repoConfigSchema.parse(parsedJson);
    ```

    This single line ensures that:
    - The KV cache hit path (line 70-73) eventually gets valid data when the cache refreshes (TTL 10 min)
    - The DB path (line 76-78) always applies Zod defaults even for pre-evidence-key configs
    - Future schema additions automatically get their defaults injected

    Import `repoConfigSchema` at the top of config.ts (line 1 already imports it — verify `defaultRepoConfig` import comes from `@shared/schema` alongside `repoConfigSchema`).

    Add comment at the `normalizeRepoConfig` function in schema.ts (line 1317) noting it does NOT inject evidence defaults — that responsibility is in config.ts's loadRepoConfig.
  </action>
  <acceptance_criteria>
    - reviewConfigSchema includes `evidence` key with `hard_drop` (boolean, default false) and `hard_drop_exempt_categories` (string array, .max(20), default ['security'])
    - repoConfigSchema inline default for review includes `evidence: { hard_drop: false, hard_drop_exempt_categories: ['security'] }`
    - jobAuditEventSchema discriminates stage literal 'evidence_hard_dropped' with fields: file, pass, droppedCount, sample (max 20, path/line/title/reason), timestamp
    - repoConfigSchema.parse({}) yields type-safe evidence.hard_drop === false
    - config.ts loadRepoConfig calls repoConfigSchema.parse(parsedJson) after model override to inject Zod defaults
    - npm run build passes (cf-typegen regeneration)
    - npm run typecheck passes (no type errors from new schema types or config.ts changes)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run test/schema-contract-inertness.spec.ts -t "repoConfigSchema" 2>/dev/null; npm run build; npm run typecheck</automated>
  </verify>
  <done>Schema compiles and typechecks; evidence.hard_drop defaults false; evidence_hard_dropped is a valid JobAuditEvent variant; config.ts injects defaults on DB-loaded config</done>
</task>

<task type="auto">
  <name>Task 2: Extract evidence check to core/evidence.ts + refactor model-output.ts to use shared function</name>
  <files>src/server/core/evidence.ts (NEW), src/server/core/model-output.ts</files>
  <read_first>
    src/server/core/model-output.ts lines 270-310 — normalizeForEvidence() and stripLeadingDiffMarkers() helpers. These are the canonical implementations.
    src/server/core/model-output.ts lines 435-556 — EVID-01 evidence check: haystack construction, needle matching, evidenceMissingEntries accumulation.
    src/server/core/model-output.ts lines 558-565 — EVID-03 buildEvidenceMissingSummary call (don't change — this is the post-extraction integration point).
    src/shared/schema.ts — ParsedReviewComment type (existingCode field, category field, path field, line field)
    src/server/core/diff.ts — FileDiff type (path, hunks properties: hunks[].lines[].content)
  </read_first>
  <action>
    Create new file `src/server/core/evidence.ts` with the shared evidence check function per D-02.

    Extract from model-output.ts the following pure functions and types:
    - `normalizeForEvidence(s: string): string` — relocate from model-output.ts line 291. Same implementation: s.replace(/\s+/g, ' ').trim().toLowerCase(). Export it.
    - `stripLeadingDiffMarkers(s: string): string` — relocate from model-output.ts line 305. Same implementation: split('\n').map(line => line.replace(/^[+-]/, '')).join('\n'). Export it.
    - `EvidenceDropEntry` interface: { path: string; line: number | null; title: string; reason: 'absent' | 'not_in_hunk' }
    - `EvidenceCheckResult` interface: { kept: ParsedReviewComment[]; dropped: ParsedReviewComment[]; entries: EvidenceDropEntry[] }
    - `checkEvidence(files: FileDiff[], comments: ParsedReviewComment[], exemptCategories: string[]): EvidenceCheckResult` — The `files` parameter type is explicitly `FileDiff[]` (matching the haystack built from `file.hunks` in model-output.ts:435-442). Implement the full logic:
      1. Build per-file haystack from FileDiff hunks (same as model-output.ts:440-442)
      2. For each finding in comments:
         a. If comment.category is in exemptCategories (case-insensitive comparison — PROHIBITION from must_haves), push to kept, continue
         b. If file not in haystack, or existingCode is null/undefined/whitespace-only (collapses to zero length after normalizeForEvidence) — drop with reason 'absent'. Whitespace-only evidence is indistinguishable from absent evidence: the model produced nothing substantive to match against.
         c. Normalize evidence + strip diff markers, test haystack.includes(needle) — if not found, drop with reason 'not_in_hunk'
         d. Otherwise — push to kept
      3. Return { kept, dropped, entries }

    The case-insensitive exemption comparison must:
    - Lowercase both the config value and the finding's category before comparing
    - e.g., `exemptCategories.some((cat) => cat.toLowerCase() === comment.category.toLowerCase())`

    Import types from `@shared/schema` (ParsedReviewComment, FileDiff). Do NOT import from model-output.ts.

    Refactor `model-output.ts` to use the shared function:
    - Remove the `normalizeForEvidence` and `stripLeadingDiffMarkers` function definitions (lines 291-307)
    - Import them from `./evidence`
    - Replace the inline evidence check (lines 435-537) with a call to `checkEvidence()`
    - The call passes `[file]` as the files array (same as before — per-file processing), empty exemptCategories (EVID-01 is audit-only, no exemptions), and only uses `result.entries` for audit accumulation
    - The `result.kept` return value is ignored — EVID-01 remains audit-only, it continues to post all findings
    - The `result.dropped` is also ignored since EVID-01 doesn't drop
    - The existing `evidenceMissingEntries.push(...)` loop is replaced by pushing `result.entries` directly
    - Verify the existing behavior is unchanged: the EVID-03 builder call at line 562 still receives the same accumulated entries

    The extracted module must maintain EXACT behavioral parity:
    - Same haystack construction (file.hunks.flatMap to .lines to .content, joined by \n, then normalizeForEvidence)
    - Same needle normalization (normalizeForEvidence(stripLeadingDiffMarkers(evidence)))
    - Same categorization logic (null/whitespace-only -> 'absent', non-empty but not found -> 'not_in_hunk')
    - Same exports so model-output.ts continues to work identically
  </action>
  <acceptance_criteria>
    - core/evidence.ts exports: normalizeForEvidence, stripLeadingDiffMarkers, EvidenceDropEntry, EvidenceCheckResult, checkEvidence(files: FileDiff[], comments: ParsedReviewComment[], exemptCategories: string[]) → EvidenceCheckResult
    - model-output.ts imports normalizeForEvidence, checkEvidence, stripLeadingDiffMarkers from ./evidence
    - model-output.ts no longer defines normalizeForEvidence or stripLeadingDiffMarkers inline
    - The EVID-01 parse-time behavior is unchanged: all findings still post (no dropping), same evidence_missing entries produced
    - No circular imports introduced (evidence.ts imports only @shared/schema types, never model-output.ts)
    - npm run typecheck passes
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run test/model-output.spec.ts -x; npx vitest run test/evidence-gate-integration.spec.ts -x; npm run typecheck</automated>
  </verify>
  <done>Existing model-output tests pass (no behavior regression); checkEvidence is importable from core/evidence.ts; normalizeForEvidence retains exact output for same input</done>
</task>

<task type="auto">
  <name>Task 3: Add buildEvidenceHardDroppedEvent to audit.ts + write test/evidence.spec.ts unit tests (17 test cases)</name>
  <files>src/server/core/audit.ts, test/evidence.spec.ts (NEW)</files>
  <read_first>
    src/server/core/audit.ts lines 587-636 — buildEvidenceMissingSummary (the exact pattern to follow for the new builder)
    src/server/core/audit.ts lines 1-8 — imports (redactFindingTitle, appendJobAuditEvents, FileReviewPass, JobAuditEvent, dateStringSchema)
    src/server/core/audit.ts line 580 — EVIDENCE_MISSING_SAMPLE_CAP constant (name the new constant EVIDENCE_HARD_DROP_SAMPLE_CAP with same value 20)
    test/noise-filter.spec.ts — pattern for unit test structure (describe/it blocks, vitest assertions)
  </read_first>
  <action>
    Part A — Builder (audit.ts):

    In `audit.ts`, add a new `EVIDENCE_HARD_DROP_SAMPLE_CAP = 20` constant near the existing `EVIDENCE_MISSING_SAMPLE_CAP` (around line 580).

    Add a new `buildEvidenceHardDroppedEvent` function after `buildEvidenceMissingSummary` (after line 636). Signature:

    ```
    export function buildEvidenceHardDroppedEvent(
      file: string,
      pass: FileReviewPass,
      entries: EvidenceDropEntry[],
    ): JobAuditEvent | null
    ```

    Implementation per D-05/D-06, following `buildEvidenceMissingSummary` pattern exactly:
    1. If entries.length === 0, return null (no zero-count event)
    2. Slice entries to EVIDENCE_HARD_DROP_SAMPLE_CAP (20)
    3. Map each entry: { path: e.path, line: e.line, title: redactFindingTitle(e.title), reason: e.reason }
    4. Build the event literal: { stage: 'evidence_hard_dropped', file, pass, droppedCount: entries.length, sample, timestamp }
    5. Use `satisfies JobAuditEvent` for compile-time type check (never cast `as`)
    6. Return the event

    Import `EvidenceDropEntry` from `./evidence`.

    Part B — Unit tests (test/evidence.spec.ts):

    Create `test/evidence.spec.ts` with 17 test cases following vitest describe/it/expect syntax. These tests cover all code paths at the unit level (checkEvidence) and schema level (buildEvidenceHardDroppedEvent), plus a cross-consistency test ensuring identity between parse-time and finalize-time haystack construction.

    **checkEvidence tests (from core/evidence.ts):**
    1. Finding with null existingCode — dropped, reason 'absent'
    2. Finding with empty/whitespace-only existingCode — dropped, reason 'absent'
    3. Finding with existingCode present in haystack — kept
    4. Finding with existingCode NOT in haystack — dropped, reason 'not_in_hunk'
    5. Finding in exempt category (e.g., 'security') — kept regardless of evidence quality
    6. Finding with exempt category entered case-insensitively: assert 'SECURITY', 'Security', and 'security' ALL bypass the drop identically (Antigravity Suggestion — covered in this test)
    7. Multiple findings, mix of dropped and kept — correct kept/dropped/entries counts
    8. Finding referencing file not in diff — dropped, reason 'absent'
    9. Relative order of surviving findings preserved
    10. exemptCategories = [] (empty array) — no exemptions, all evidence-invalid findings dropped
    11. exemptCategories = undefined — defaults to ['security'] (Zod default via checkEvidence) — security findings with bad evidence kept
    12. Cross-consistency: assert that checkEvidence called with the same `[FileDiff]` and same `ParsedReviewComment.existingCode` produces the same haystack as the inline EVID-01 code in model-output.ts. Test by constructing a known FileDiff with known hunks, running both the old inline algorithm (before extraction) and the new checkEvidence, and asserting byte-identical haystack output. Since the old code is removed, the test should construct a minimal reproduction: given a FileDiff with hunk lines and a finding with known existingCode, the drop/kept outcome of checkEvidence matches the expected outcome from the old algorithm's behavior.

    **buildEvidenceHardDroppedEvent tests (from audit.ts):**
    13. Empty entries array — returns null
    14. Single entry — returns event with droppedCount=1, sample has 1 entry
    15. Multiple entries (e.g., 25) — sample capped at 20, droppedCount=25
    16. Event shape matches schema (round-trip through jobAuditEventSchema.parse)
    17. Each sample entry carries reason ('absent' or 'not_in_hunk') — assert both variants work

    Mock data: construct FileDiff objects with path and hunks (minimal — one file, one hunk, several lines). Construct ParsedReviewComment objects with path, line, category, existingCode, title fields.

    For the cross-consistency test (12), construct a FileDiff with known content and assert that the haystack produced by checkEvidence's internal haystack construction matches the expected normalized string. This is a standalone regression test: if the haystack construction logic diverges between evidence.ts and any future code, this test catches it.
  </action>
  <acceptance_criteria>
    - buildEvidenceHardDroppedEvent exists in audit.ts, exported
    - Returns null for empty entries (no zero-count event)
    - Returns correctly shaped event for populated entries
    - Sample capped at 20 entries
    - Each sample entry has title (redactFindingTitle-processed) + reason ('absent' | 'not_in_hunk')
    - checkEvidence correctly classifies findings as kept/dropped per evidence quality
    - Exempt categories bypass the drop (including 'SECURITY', 'Security', 'security' variants)
    - Cross-consistency test asserts byte-identical haystack between extracted and reference construction
    - exemptCategories = [] drops all evidence-invalid findings; exemptCategories = undefined defaults to ['security']
    - All 17 tests pass: npx vitest run test/evidence.spec.ts
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run test/evidence.spec.ts -x; npm run typecheck</automated>
  </verify>
  <done>All 17 unit tests pass; buildEvidenceHardDroppedEvent produces schema-valid events; checkEvidence returns correct kept/dropped/entries; cross-consistency asserted</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Config input (parsed_json) → review pipeline | The `hard_drop_exempt_categories` array from repo config is untrusted user input that controls whether findings are dropped |
| Config loading (config.ts) → runtime | DB configs may lack keys added after they were stored; missing defaults must be injected at load time |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-01 | Tampering | reviewConfigSchema, evidence config block | high | mitigate | Zod `.default(false)` ensures the toggle is off by default; `repoConfigSchema.parse()` in config.ts (Task 1 Part B) injects defaults for DB-loaded configs missing the `evidence` key — runtime code gate (`?? false`) serves as second line of defense |
| T-26-03 | Tampering | core/evidence.ts checkEvidence exempt-category comparison | high | mitigate | Case-insensitive comparison (`cat.toLowerCase() === comment.category.toLowerCase()`) prevents silent bypass of exemptions — enforced via plan prohibition in must_haves |
| T-26-02 | Information Disclosure | audit.ts buildEvidenceHardDroppedEvent | medium | mitigate | `redactFindingTitle` applied to all sample entry titles; builder never passes body/diff/existingCode/codeSuggestion to audit event |
| T-26-04 | Repudiation | review.ts finalize audit emission | low | accept | At-most-once gating exists in Plan 26-02 (`!finalizeRetriedPastPost`) preventing duplicate events; retry loss is accepted (best-effort telemetry) |
| T-26-06 | Tampering | config.ts loadRepoConfig parse injection | medium | mitigate | `repoConfigSchema.parse(parsedJson)` at config load time ensures Zod defaults are always applied even for pre-evidence DB configs; type-safe via compiled Zod schema |
| T-26-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages in this phase — zero npm dependencies added |

</threat_model>

<verification>
- npx vitest run test/evidence.spec.ts -x — all 17 unit tests pass
- npx vitest run test/model-output.spec.ts -x — existing EVID-01 tests pass (no regression)
- npx vitest run test/evidence-gate-integration.spec.ts -x — existing evidence gate integration tests pass
- npm run build — Vite + cf-typegen succeed
- npm run typecheck — tsc --noEmit passes (config.ts loadRepoConfig changes must compile)
</verification>

<success_criteria>
1. Schema changes compile: evidence config block with .max(20) array bound + evidence_hard_dropped audit event variant + inline default literal
2. config.ts loadRepoConfig calls repoConfigSchema.parse() after model override to inject Zod defaults
3. core/evidence.ts exports checkEvidence with correct FileDiff[] signature
4. model-output.ts uses shared checkEvidence (no inline duplicate) — existing tests pass unchanged
5. buildEvidenceHardDroppedEvent in audit.ts produces correct shape
6. All 17 unit tests pass, no existing test regressions
7. Schema push verification: npm run build && npm run typecheck
</success_criteria>

<artifacts_this_phase_produces>
- schema.ts: `evidence` config block in reviewConfigSchema (hard_drop, hard_drop_exempt_categories with .max(20))
- schema.ts: `evidence_hard_dropped` variant in jobAuditEventSchema (stage literal, file, pass, droppedCount, sample capped at 20, timestamp)
- schema.ts: inline default literal `evidence: { hard_drop: false, hard_drop_exempt_categories: ['security'] }` in repoConfigSchema review default
- config.ts: repoConfigSchema.parse() call in loadRepoConfig after model override to inject Zod defaults for all new/old configs
- core/evidence.ts (NEW): `checkEvidence(files: FileDiff[], ...)` function, `EvidenceCheckResult` interface, `EvidenceDropEntry` interface, `normalizeForEvidence()` export
- core/audit.ts: `buildEvidenceHardDroppedEvent()` function
- test/evidence.spec.ts (NEW): 17 unit tests covering checkEvidence behavior, cross-consistency, and buildEvidenceHardDroppedEvent shape
</artifacts_this_phase_produces>

<output>
Create .planning/phases/26-evid-02-hard-drop-evidence-gate/26-01-SUMMARY.md when done
</output>
