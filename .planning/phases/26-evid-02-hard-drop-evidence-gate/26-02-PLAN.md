---
phase: 26-evid-02-hard-drop-evidence-gate
plan: 02
type: execute
wave: 2
depends_on:
  - 26-01
files_modified:
  - src/server/core/review.ts
  - test/review-flow.spec.ts
  - src/client/components/features/repos/review-settings-panel.tsx
  - src/client/lib/audit-grouping.ts
  - src/client/components/features/job-detail/audit-trail-viewer.tsx
autonomous: true
requirements:
  - EVID-02
must_haves:
  truths:
    - "drop-before-dedup: a hard-dropped finding does not participate in dedup. When a dropped finding and a kept finding have identical content, only the kept finding reaches dedup — no collision or merge ambiguity."
    - "When all findings in a file are hard-dropped, the file produces zero posted findings. The audit event still fires with droppedCount > 0 and a populated sample, so the drop is explainable even when output is empty."
    - "Hard-drop is a filter (in-memory removal before dedup), not a reorder. The relative order of surviving findings is preserved because the drop iterates the findings array and removes non-matching entries in place."
    - "The hard-drop check is deterministic: same diff haystack + same findings produce identical drop decisions."
    - "The hard-drop is an in-memory filter in the finalize phase. Retries re-run against immutable file_reviews producing identical results. Audit event uses at-most-once gating preventing duplicate events on retry."
    - "Config-gated: when evidence.hard_drop is false (default), the hard-drop code path is skipped entirely and finalize produces byte-identical output to today (NREG-01)."
  prohibitions:
    - "The hard_drop_exempt_categories check MUST compare categories case-insensitively. A config entry 'Security' or 'SECURITY' must match the reviewCategories value 'security'. A case-sensitive comparison would silently bypass exemptions."
  artifacts:
    - review.ts: evidence hard-drop gate insertion in runFinalizePhase (before dedup, after reviewedComments resolution)
    - review-flow.spec.ts: integration tests for end-to-end evidence hard-drop
    - review-settings-panel.tsx: evidence section UI (hard_drop toggle + hard_drop_exempt_categories editor)
    - audit-grouping.ts: evidence_hard_dropped display stage normalization (maps to evidence_missing display group)
    - audit-trail-viewer.tsx: evidence_hard_dropped DecisionEvent renderer case
---

<objective>
Wire the evidence hard-drop gate into the finalize phase of review.ts, add integration tests, and provide dashboard surfaces (config UI + audit viewer renderer).

Purpose: The building blocks from Plan 26-01 (checkEvidence, buildEvidenceHardDroppedEvent, schema types) are now consumed in finalize to actually drop hallucinated-evidence findings before dedup. Integration tests verify behavior end-to-end. Dashboard surfaces make the toggle visible to operators and the resulting events viewable in the audit trail.

Output: Modified finalize phase, integration tests, dashboard config section, audit viewer renderer.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-evid-02-hard-drop-evidence-gate/26-CONTEXT.md
@.planning/phases/26-evid-02-hard-drop-evidence-gate/26-RESEARCH.md

# Source files for patterns and context
@src/server/core/review.ts
@src/server/core/audit.ts
@src/server/core/evidence.ts
@src/client/components/features/repos/review-settings-panel.tsx
@src/client/lib/audit-grouping.ts
@src/client/components/features/job-detail/audit-trail-viewer.tsx
@test/review-flow.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire evidence hard-drop gate into finalize phase in review.ts</name>
  <files>src/server/core/review.ts</files>
  <read_first>
    src/server/core/review.ts lines 2174-2183 — reviewedComments resolution (the candidate set that feeds dedup). This is where the drop gate inserts: right after reviewedComments is resolved.
    src/server/core/review.ts lines 2280-2288 — applyNoiseFilter call. The evidence drop must complete before this.
    src/server/core/review.ts lines 2390-2452 — finalizeRetriedPastPost gate pattern. The audit emission follows this at-most-once pattern exactly.
    src/server/core/review.ts lines 2080-2082 — files loading (getJobDiffFiles) and review loading (getFileReviewsForJobs). Both are already available in scope.
    src/server/core/review.ts line 2169-2170 — config access pattern: `config.review.dedup?.enabled ?? true`. The evidence toggle uses the same pattern: `config.review.evidence?.hard_drop ?? false`.
    src/server/core/review.ts function signature line 2058 — runFinalizePhase signature, showing the parameters in scope (env, job, vcs, formatter)
    src/server/core/audit.ts — buildEvidenceHardDroppedEvent and appendJobAuditEvents
    src/server/core/evidence.ts — checkEvidence, EvidenceDropEntry
  </read_first>
  <action>
    Insert the evidence hard-drop gate into `runFinalizePhase` in `review.ts`. The insertion point is immediately after `reviewedComments` is resolved at line 2183 and before the `noiseFilterOptions` construction at line 2280.

    ---

    **Step 1: Import checkEvidence and buildEvidenceHardDroppedEvent**

    At the top of review.ts, add imports:
    - `import { checkEvidence } from './evidence';`
    - `import { buildEvidenceHardDroppedEvent, type EvidenceDropEntry } from './audit';`

    (Verify `appendJobAuditEvents` is already imported from `@server/db/jobs` — it is used elsewhere in the file.)

    **Step 2: Insert evidence drop code block**

    After line 2183 (`reviewedComments = ...`), add the following block:

    ```
    // EVID-02: evidence hard-drop gate (Plan 26-02). Runs BEFORE dedup (D-01) so a hallucinated-evidence
    // finding never participates in merging. Only active when config.evidence.hard_drop is true (NREG-01).
    // Uses the shared checkEvidence function (same EVID-01 haystack algorithm) to re-check existingCode
    // against the diff.
    if (config.review.evidence?.hard_drop ?? false) {
      const exemptCategories = config.review.evidence?.hard_drop_exempt_categories ?? ['security'];
      const evidenceResult = checkEvidence(files, reviewedComments, exemptCategories);
      reviewedComments = evidenceResult.kept;

      // Emit evidence_hard_dropped audit events per (file, pass) unit.
      // At-most-once: gated on !finalizeRetriedPastPost to prevent duplicates on retry (same pattern
      // as recordFinalizeDrops at line 2425). Best-effort: never fails the caller.
      if (evidenceResult.entries.length > 0 && !finalizeRetriedPastPost) {
        const entriesByFileAndPass = new Map<string, EvidenceDropEntry[]>();
        for (const review of reviews) {
          const reviewEntries = evidenceResult.entries.filter(
            (e) => e.path === review.file_path,
          );
          if (reviewEntries.length === 0) continue;
          const key = `${review.file_path}:${review.pass}`;
          const existing = entriesByFileAndPass.get(key) ?? [];
          entriesByFileAndPass.set(key, [...existing, ...reviewEntries]);
        }
        for (const [key, fileEntries] of entriesByFileAndPass) {
          const [filePath, pass] = key.split(':') as [string, FileReviewPass];
          const event = buildEvidenceHardDroppedEvent(filePath, pass, fileEntries);
          if (event) {
            try {
              await appendJobAuditEvents(env, job.id, [event]);
            } catch (error) {
              // Best-effort: log and continue (same posture as recordUnitAudit).
              logger.warn(`Failed to record evidence_hard_dropped event for job ${job.id}`, error);
            }
          }
        }
      }
    }
    ```

    The `FileReviewPass` type is already imported at the top of review.ts. `appendJobAuditEvents` is already imported. `logger` is already imported.

    **Step 3: Verify the insertion point**

    The block goes between the closing of the `reviewedComments` assignment (line 2183 `}` or `);`) and the `const mainReviews = ...` line (line 2190). Specifically:

    - After line 2183 (`reviewedComments = reviews.flatMap(...)`) but before line 2190 (`const mainReviews = ...`)
    - This ensures the drop happens BEFORE `composedFloors` (line 2217), `noiseFilterOptions` (line 2280), and `applyNoiseFilter` (line 2288)
    - The `config` variable is already in scope (line 2081)
    - The `files` variable is already in scope (line 2082)
    - The `reviews` variable is already in scope (line 2083)
    - The `finalizeRetriedPastPost` variable is NOT yet in scope (it's defined at line 2410). Move the `finalizeRetriedPastPost` variable declaration to BEFORE the evidence drop block, or define it earlier. Actually, looking at the code flow: `finalizeRetriedPastPost` checks `job.steps` which is already loaded — it's safe to compute early. But to minimize churn, use a different approach: define a local gate variable inline.

    **Better approach:** Define `finalizeRetriedPastPost` right before the evidence block. The variable was first used at line 2410 — we can compute it earlier:

    ```
    const finalizeRetriedPastPost = !!(job.steps?.some(
      (step) => step.name === 'Completing' && (step.status === 'running' || step.status === 'done'),
    ));
    ```

    But this duplicates the logic from line 2410. Instead, compute it once early and use it in both places. Extract:

    ```
    const finalizeRetriedPastPost = job.steps.some(
      (step) => step.name === 'Completing' && (step.status === 'running' || step.status === 'done'),
    );
    ```

    Place this declaration right before the evidence drop block. Remove the duplicate definition at line 2410-2412 and replace with just `const` → reuse. Actually, to minimize the diff, simply define `finalizeRetriedPastPost` as a `let` and set it early, or compute it inline.

    **Simpler approach:** Insert the evidence block right after line 2183. For the at-most-once gate, compute `finalizeRetriedPastPost` inline within the block:

    ```
    const alreadyReachedPost = job.steps.some(
      (step) => step.name === 'Completing' && (step.status === 'running' || step.status === 'done'),
    );
    ```

    Then at line 2410, the existing `const finalizeRetriedPastPost = ...` declaration still works because it's a separate const in a different block scope (or same scope — need to verify). Actually, both are in the same function scope. So we need to either:
    a) Import `finalizeRetriedPastPost` from the existing usage at line 2410 (move declaration earlier), or
    b) Use a differently-named variable within the evidence block

    Let's go with option (b) to minimize changes to the existing code:

    ```
    if (config.review.evidence?.hard_drop ?? false) {
      // ... drop logic ...
      
      // At-most-once audit gate: skip if a prior finalize invocation already reached the posting stage.
      const evidenceAuditGate = !job.steps.some(
        (step) => step.name === 'Completing' && (step.status === 'running' || step.status === 'done'),
      );
      
      if (evidenceResult.entries.length > 0 && evidenceAuditGate) {
        // ... emit events ...
      }
    }
    ```

    This avoids any conflict with the existing `finalizeRetriedPastPost` at line 2410.
  </action>
  <acceptance_criteria>
    - When `evidence.hard_drop` is false (default), the code block is skipped and reviewedComments is unchanged (NREG-01)
    - When `evidence.hard_drop` is true, findings with absent/not_in_hunk existingCode are removed from reviewedComments
    - Exempt categories bypass the drop (case-insensitive)
    - `evidence_hard_dropped` audit events are emitted per (file, pass) with correct droppedCount and sample
    - The audit emission is gated: a retry that already reached Completing does not re-emit
    - The drop happens BEFORE the fileSummaries/composedFloors/applyNoiseFilter computations
    - npm run build && npm run typecheck pass
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck; npm run build</automated>
  </verify>
  <done>Hard-drop gate compiles and is skipped when toggle is off; code review confirms insertion point is before dedup</done>
</task>

<task type="auto">
  <name>Task 2: Write integration test for evidence hard-drop in review-flow.spec.ts</name>
  <files>test/review-flow.spec.ts</files>
  <read_first>
    test/review-flow.spec.ts — read the first 50 lines and the last existing test case to understand test structure, helpers (createTestEnv, mockGitHub, etc.) and assertions pattern. Search for "evidence" to find existing evidence-related tests.
    src/server/core/evidence.ts — checkEvidence function API
    src/server/core/review.ts — finalize phase flow
  </read_first>
  <action>
    Add integration tests to `test/review-flow.spec.ts` for the evidence hard-drop gate. Follow the existing test patterns in this file (mock GitHub, use createTestEnv, etc.).

    Add test cases:

    **Test 1: evidence hard-drop removes findings with bad existingCode**
    - Set up a review job with `evidence.hard_drop: true` in config
    - Configure mock model output where some findings have hallucinated `existing_code` (not found in diff)
    - Run the full review pipeline
    - Assert: evidence-invalid findings are NOT in the posted comments
    - Assert: audit events contain `evidence_hard_dropped` with correct droppedCount

    **Test 2: exempt categories bypass hard-drop**
    - Same setup as Test 1, but the hallucinated-evidence finding has category 'security'
    - Run the full review pipeline
    - Assert: the security finding with bad evidence STILL posts
    - Assert: `evidence_hard_dropped` droppedCount excludes the exempt finding

    **Test 3: hard-drop disabled produces byte-identical output (NREG-01)**
    - Set up a job with evidence-invalid findings and `evidence.hard_drop: false` (default)
    - Run the review pipeline
    - Assert: all findings post (same as today's behavior)
    - Assert: no `evidence_hard_dropped` audit events emitted

    Test structure pattern (from existing tests):
    - Use `createTestEnv()` or equivalent test fixtures
    - Use `mockGitHub()` or equivalent VCS mock
    - Use `describe('evidence hard-drop', () => { ... })` to group tests
    - Use `it('should ...', async () => { ... })` for individual test cases
    - Use `expect(...)` for assertions
    - Test config should set `evidence.hard_drop: true` via the configSnapshot passed to the pipeline

    For the model output, create a file review response where some findings have genuine `existing_code` matching diff lines and others have hallucinated `existing_code` (random text not in the diff). The test against `evidence_hard_dropped` events should check the audit trail (job.audit array parsed from DB).

    Check the existing test file for patterns like:
    - How config overrides are passed to the review pipeline
    - How model responses are injected
    - How posted comments are retrieved for assertion
    - How audit events are read after pipeline completion
  </action>
  <acceptance_criteria>
    - Test 1 passes: evidence-invalid findings dropped when hard_drop=true
    - Test 2 passes: exempt categories bypass drop
    - Test 3 passes: byte-identical output when hard_drop=false (NREG-01)
    - All tests pass as part of the full `npm test` suite (not flaky)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run test/review-flow.spec.ts -t "evidence hard-drop" -x</automated>
  </verify>
  <done>Three integration tests pass — evidence drop, exempt categories, and NREG-01 disabled mode</done>
</task>

<task type="auto">
  <name>Task 3: Add dashboard config UI for evidence section + minimal audit viewer renderer</name>
  <files>src/client/components/features/repos/review-settings-panel.tsx, src/client/lib/audit-grouping.ts, src/client/components/features/job-detail/audit-trail-viewer.tsx</files>
  <read_first>
    src/client/components/features/repos/review-settings-panel.tsx — Full file (728 lines). Study:
    - Lines 228-276: state declarations for all toggle blocks. Add evidence state vars here.
    - Lines 284-316: the `review` object assembly (immutable spreads). Add evidence spread here.
    - Lines 327-353: dirty detection. Add evidence fields to dirty comparison.
    - Lines 358-391: useEffect deps. Add evidence state vars to dependency array.
    - Lines 600-623: "Advanced" toggle section with all advanced toggles. Add evidence section here.
    - Lines 117-180: ListEditor component (used for exempt-categories list).
    - Lines 183-202: ToggleRow component (used for the hard_drop master toggle).

    src/client/lib/audit-grouping.ts lines 65-75 — normalizeAuditDisplayStage. Add evidence_hard_dropped mapping.

    src/client/components/features/job-detail/audit-trail-viewer.tsx lines 150-193 — evidence_missing_summary renderer case. Follow this exact pattern for the new evidence_hard_dropped renderer.

    src/client/components/features/job-detail/audit-trail-viewer.tsx lines 15-27 — STAGE_LABELS. No change needed (evidence_hard_dropped maps to evidence_missing display group).
  </read_first>
  <action>
    ### Part A: Review Settings Panel (review-settings-panel.tsx)

    Add evidence section UI to the "Advanced" section of ReviewSettingsPanel:

    1. **State variables** (around line 276, after `escalateFloors`):
       ```
       const [evidenceHardDrop, setEvidenceHardDrop] = useState(current.evidence?.hard_drop ?? false);
       const [evidenceExemptCategories, setEvidenceExemptCategories] = useState<string[]>(
         current.evidence?.hard_drop_exempt_categories ?? ['security'],
       );
       ```

    2. **Review object spread** (around line 315, after `rounds` spread):
       ```
       evidence: {
         hard_drop: evidenceHardDrop,
         hard_drop_exempt_categories: evidenceExemptCategories,
       },
       ```

    3. **Dirty detection** (around line 353, after `escalateFloors`):
       ```
       evidenceHardDrop !== (current.evidence?.hard_drop ?? false) ||
       !orderedListEqual(evidenceExemptCategories, current.evidence?.hard_drop_exempt_categories ?? ['security']) ||
       ```

    4. **useEffect deps** (around line 388, after `escalateFloors`):
       Add `evidenceHardDrop` and `evidenceExemptCategories` to the deps array.

    5. **UI in Advanced section** (inside the `{advancedOpen && (` block, around line 722, after the escalate floors ToggleRow):
       ```
       <ToggleRow
         label="Evidence hard-drop"
         description="Drop findings whose existing_code evidence is not found in the hunk (hallucinated evidence)."
         checked={evidenceHardDrop}
         onCheckedChange={setEvidenceHardDrop}
         ariaLabel="Toggle evidence hard-drop"
       />
       <ListEditor
         title="Exempt categories"
         hint="Categories that always post findings regardless of evidence quality. Default: security."
         items={evidenceExemptCategories}
         onChange={setEvidenceExemptCategories}
         placeholder="e.g. correctness"
         addAriaLabel="New exempt category"
         emptyHint="All categories are subject to hard-drop. No exemptions."
       />
       ```

    ### Part B: Audit grouping (audit-grouping.ts)

    In `normalizeAuditDisplayStage` (around line 73, after the `evidence_missing_summary` mapping), add:
    ```
    if (stage === 'evidence_hard_dropped') return 'evidence_missing';
    ```

    This maps the new event stage to the existing `evidence_missing` display group. The STAGE_ORDER entry at `'evidence_missing'` already exists (line 20). No change to STAGE_ORDER needed — `evidence_hard_dropped` and `evidence_missing_summary` both land in the same display group alongside legacy `evidence_missing` per-finding events.

    ### Part C: Audit viewer renderer (audit-trail-viewer.tsx)

    In the `DecisionEvent` switch (around line 193, after the `evidence_missing_summary` case), add a new `evidence_hard_dropped` case:

    ```
    case 'evidence_hard_dropped':
      return (
        <li className="rounded-md border border-border/40 bg-card/40 p-3">
          <MetricLine label="file" value={event.file} />
          <MetricLine label="pass" value={event.pass} />
          <MetricLine label="dropped" value={String(event.droppedCount)} />
          {event.sample.length > 0 && (
            <ul className="mt-2 flex flex-col gap-1.5 border-t border-border/30 pt-2">
              {event.sample.map((s, i) => (
                <li key={i}>
                  <SampleIdentifier path={s.path} line={s.line} title={s.title} />
                  <div className="text-[10px] text-muted-foreground/70 font-mono">{s.reason}</div>
                </li>
              ))}
            </ul>
          )}
        </li>
      );
    ```

    The renderer follows the `evidence_missing_summary` case pattern exactly: file/pass as identifier, droppedCount as a metric line, and up to 20 sample entries with path/line/title and reason tag. The `default: return null` at line 347 already handles unknown stages — no additional work needed for fallback.
  </action>
  <acceptance_criteria>
    - ReviewSettingsPanel shows evidence hard-drop toggle in Advanced section
    - Toggle off = hard-drop disabled (default). Toggle on = hard-drop enabled.
    - Exempt categories list defaults to ['security']
    - ListEditor allows adding/removing exempt categories
    - The assembled review object includes `evidence: { hard_drop, hard_drop_exempt_categories }`
    - Dirty tracking detects changes to evidence settings
    - npm run build passes (Vite client build)
    - audit-trail-viewer renders evidence_hard_dropped events with droppedCount and sample entries
    - STAGE_LABELS and audit-grouping map evidence_hard_dropped to evidence_missing display group
  </acceptance_criteria>
  <verify>
    <automated>npm run build; npm run typecheck</automated>
  </verify>
  <done>Dashboard config UI renders evidence section; audit viewer renders evidence_hard_dropped events</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Config input → review pipeline | Config toggle controls whether evidence hard-drop runs; untrusted if config source is compromised |
| Audit events → persistence | Sample entry titles must not leak content beyond privacy-bounded identifiers |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-01 | Tampering | review.ts evidence toggle gate | high | mitigate | `?? false` guard ensures hard-drop is off when config is missing/falsy; code review of the `if (config.review.evidence?.hard_drop ?? false)` gate |
| T-26-03 | Tampering | core/evidence.ts exempt-category comparison | high | mitigate | Case-insensitive comparison enforced in checkEvidence (Plan 26-01 Task 2) |
| T-26-02 | Information Disclosure | audit-trail-viewer.tsx evidence_hard_dropped renderer | medium | mitigate | Title already redacted by redactFindingTitle at production time (audit.ts builder); renderer only displays the pre-redacted field |
| T-26-04 | Repudiation | review.ts audit emission | high | mitigate | At-most-once gate (`!evidenceAuditGate`) prevents duplicate events on retry (same pattern as recordFinalizeDrops) |
| T-26-05 | Denial of Service | review-settings-panel.tsx config editor | low | accept | Config is persisted via existing PATCH route with Zod validation; no new server-side risk |
| T-26-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages in this phase |
</threat_model>

<verification>
- npm run typecheck — tsc --noEmit passes (no type errors from evidence block, new state vars, or renderer)
- npm run build — Vite + cf-typegen succeed (client build includes new evidence UI section)
- npx vitest run test/review-flow.spec.ts -x — integration test suite passes including new evidence hard-drop tests
- Manual confirmation: code review of insertion point (before dedup, after reviewedComments)
</verification>

<success_criteria>
1. Evidence hard-drop gate compiles in review.ts and is skipped when toggle is off (NREG-01)
2. Gate runs before dedup, after reviewedComments resolution
3. Three integration tests pass: drop behavior, exempt categories, NREG-01 disabled mode
4. Dashboard config UI shows evidence toggle + exempt categories list editor
5. Audit viewer renders evidence_hard_dropped events with droppedCount and sample
6. npm run build && npm run typecheck pass
</success_criteria>

<artifacts_this_phase_produces>
- review.ts: evidence hard-drop gate block in runFinalizePhase (early return path gated on `config.review.evidence?.hard_drop`), at-most-once audit emission logic
- review-flow.spec.ts: integration test group 'evidence hard-drop' with 3 test cases
- review-settings-panel.tsx: `evidenceHardDrop` + `evidenceExemptCategories` state, evidence section UI in Advanced panel
- audit-grouping.ts: `evidence_hard_dropped` → `evidence_missing` mapping in normalizeAuditDisplayStage
- audit-trail-viewer.tsx: `case 'evidence_hard_dropped':` renderer in DecisionEvent switch
</artifacts_this_phase_produces>

<output>
Create .planning/phases/26-evid-02-hard-drop-evidence-gate/26-02-SUMMARY.md when done
</output>
