---
phase: 26-evid-02-hard-drop-evidence-gate
plan: 02
type: execute
wave: 2
depends_on:
  - 26-01
files_modified:
  - src/server/core/review.ts
  - src/server/routes/api/repos.ts
  - test/review-flow.spec.ts
  - src/client/components/features/repos/review-settings-panel.tsx
  - src/client/lib/audit-grouping.ts
  - src/client/components/features/job-detail/audit-trail-viewer.tsx
autonomous: true
requirements:
  - EVID-02
must_haves:
  truths:
    - "drop-before-dedup: a hard-dropped finding does not participate in dedup. When a dropped finding and a kept finding have identical content, only the kept finding reaches dedup — no collision or merge ambiguity."
    - "When all findings in a file are hard-dropped, the file produces zero posted findings. The audit event still fires with droppedCount > 0 and a populated sample, so the drop is explainable even when output is empty."
    - "Hard-drop is a filter (in-memory removal before dedup), not a reorder. The relative order of surviving findings is preserved because the drop iterates the findings array and removes non-matching entries in place."
    - "The hard-drop check is deterministic: same diff haystack + same findings produce identical drop decisions."
    - "The hard-drop is an in-memory filter in the finalize phase. Retries re-run against immutable file_reviews producing identical results. Audit event uses at-most-once gating via persisted `finalizeRetriedPastPost` check to prevent duplicate events on retry (not an in-memory variable that resets on workflow retry — CRITICAL PATH FIX #2)."
    - "Config-gated: when evidence.hard_drop is false (default), the hard-drop code path is skipped entirely and finalize produces byte-identical output to today (NREG-01)."
    - "API shallow-merge fix: PATCH /api/repos/:owner/:repo/config uses deep-merge for the `review` sub-object so patching `evidence` does not destroy other review settings like `dedup`, `passes`, `rounds` (CRITICAL PATH FIX #3)."
    - "Dashboard config UI normalizes exempt categories to lowercase on save so 'Security' or 'SECURITY' stored values never cause silent case-sensitivity mismatches in the exempt-category comparison."
  prohibitions:
    - "The hard_drop_exempt_categories check MUST compare categories case-insensitively. A config entry 'Security' or 'SECURITY' must match the reviewCategories value 'security'. A case-sensitive comparison would silently bypass exemptions."
  artifacts:
    - review.ts: evidence hard-drop gate insertion in runFinalizePhase (before dedup, after reviewedComments resolution), using persisted finalizeRetriedPastPost check for at-most-once audit emission
    - repos.ts: deep-merge fix for `review` sub-object in PATCH config handler
    - review-flow.spec.ts: integration tests for end-to-end evidence hard-drop
    - review-settings-panel.tsx: evidence section UI (hard_drop toggle + hard_drop_exempt_categories list editor with lowercase normalization)
    - audit-grouping.ts: evidence_hard_dropped display stage normalization (maps to evidence_missing display group)
    - audit-trail-viewer.tsx: evidence_hard_dropped DecisionEvent renderer case
---

<objective>
Wire the evidence hard-drop gate into the finalize phase of review.ts with correct at-most-once gating, fix the API shallow-merge bug in the config PATCH route, add integration tests, and provide dashboard surfaces (config UI with lowercase normalization + audit viewer renderer).

Purpose: The building blocks from Plan 26-01 (checkEvidence, buildEvidenceHardDroppedEvent, schema types, config default injection) are now consumed in finalize to actually drop hallucinated-evidence findings before dedup. Integration tests verify behavior end-to-end. Dashboard surfaces make the toggle visible to operators and the resulting events viewable in the audit trail.

Output: Modified finalize phase with gated audit emission, fixed PATCH route, integration tests, dashboard config section, audit viewer renderer.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-evid-02-hard-drop-evidence-gate/26-CONTEXT.md
@.planning/phases/26-evid-02-hard-drop-evidence-gate/26-RESEARCH.md
@.planning/phases/26-evid-02-hard-drop-evidence-gate/26-REVIEWS.md
@.planning/phases/26-evid-02-hard-drop-evidence-gate/26-01-PLAN.md

# Source files for patterns and context
@src/server/core/review.ts
@src/server/core/audit.ts
@src/server/core/evidence.ts
@src/server/routes/api/repos.ts
@src/client/components/features/repos/review-settings-panel.tsx
@src/client/lib/audit-grouping.ts
@src/client/components/features/job-detail/audit-trail-viewer.tsx
@test/review-flow.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire evidence hard-drop gate in review.ts + fix API shallow-merge in repos.ts</name>
  <files>src/server/core/review.ts, src/server/routes/api/repos.ts</files>
  <read_first>
    src/server/core/review.ts lines 2174-2183 — reviewedComments resolution (the candidate set that feeds dedup). This is where the drop gate inserts.
    src/server/core/review.ts lines 2406-2412 — finalizeRetriedPastPost computation. CRITICAL: this must be MOVED before the evidence block (line 2183) so the evidence block can reference it.
    src/server/core/review.ts lines 2425-2433 — recordFinalizeDrops gate (uses finalizeRetriedPastPost). After moving the computation early, remove the duplicate at line 2410-2412.
    src/server/core/review.ts lines 2080-2083 — files and reviews loading. The moved finalizeRetriedPastPost computation goes after these lines.
    src/server/core/review.ts lines 2169-2170 — config access pattern: `config.review.dedup?.enabled ?? true`. The evidence toggle uses same pattern.
    src/server/core/review.ts lines 1-39 — current imports. Add appendJobAuditEvents to the @server/db/jobs import.
    src/server/core/audit.ts — buildEvidenceHardDroppedEvent
    src/server/core/evidence.ts — checkEvidence, EvidenceDropEntry
    src/server/routes/api/repos.ts lines 163-166 — the shallow-merge bug: `{ ...existing.parsedJson, ...configPatch }` replaces entire review object.
  </read_first>
  <action>
    Part A — Evidence hard-drop gate in review.ts (CRITICAL PATH FIX #2, #13, #11):

    This is the core wiring task. Three changes to review.ts:

    **Change 1 — Import additions:**
    At the top of review.ts (after existing imports from `./audit` around line 39), add:
    ```
    import { checkEvidence, type EvidenceDropEntry } from './evidence';
    import { buildEvidenceHardDroppedEvent } from './audit';
    ```
    And add `appendJobAuditEvents` to the `@server/db/jobs` import block (around line 30).

    **Change 2 — Move finalizeRetriedPastPost computation early:**
    The `finalizeRetriedPastPost` variable is currently computed at line 2410-2412, AFTER the evidence block insertion point (~2183). Per REVIEWS.md CRITICAL PATH FIX #2, the evidence audit emission MUST use the persisted `finalizeRetriedPastPost` check (not an in-memory `evidenceAuditGate` variable that resets on workflow retry).

    Move the computation to right after reviews are fully loaded and reconciled (after line 2134, after the `reviews = await getFileReviewsForJobs(env, [job.id]);` call at line 2127 or the `reviews` variable is final at line 2083):

    Insert after the `reviews = await getFileReviewsForJobs(env, [job.id]);` call that follows bulkMarkFilesFailed (around line 2127). Actually, the safest earliest point is after the `Reviewing Files` step is marked done at line 2141 — all reviews are loaded and reconciled at that point, and no evidence-related I/O happens before it. Insert at line 2142:

    ```
    // REVIEWS CRITICAL FIX #2: moved from line 2410 so the EVID-02 evidence block (inserted at line 2183)
    // can use the persisted finalizeRetriedPastPost gate for at-most-once audit emission. Computing this
    // early is safe because job.steps is immutable after job creation — the Workflow steps are set once.
    const finalizeRetriedPastPost = job.steps.some(
      (step) => step.name === 'Completing' && (step.status === 'running' || step.status === 'done'),
    );
    ```

    Then at line 2410-2412, REMOVE the duplicate `const finalizeRetriedPastPost = ...` declaration. The existing usage at line 2425 (`if (!finalizeRetriedPastPost)`) continues to work since the variable is now scoped to the function body from the earlier position.

    **Change 3 — Insert evidence drop gate block (REVIEWS CRITICAL PATH FIX #2, #11, #13):**

    After line 2183 (after `reviewedComments = ...` is resolved) and before line 2190 (`const mainReviews = ...`), insert:

    ```
    // EVID-02: evidence hard-drop gate (Plan 26-02). Runs BEFORE dedup (D-01) so a hallucinated-evidence
    // finding never participates in merging. Only active when config.evidence.hard_drop is true (NREG-01).
    // Uses the shared checkEvidence function (same EVID-01 haystack algorithm) to re-check existingCode
    // against the diff.
    if (config.review.evidence?.hard_drop ?? false) {
      const exemptCategories = config.review.evidence?.hard_drop_exempt_categories ?? ['security'];
      const evidenceResult = checkEvidence(files, reviewedComments, exemptCategories);
      reviewedComments = evidenceResult.kept;

      // At-most-once audit emission: gated on !finalizeRetriedPastPost so a retry that already reached
      // 'Completing' does not re-append evidence_hard_dropped events (same pattern as recordFinalizeDrops
      // at line 2425). The gate is the persisted job.steps check, NOT an in-memory variable (REVIEWS
      // CRITICAL FIX #2 — an in-memory guard resets on workflow retry).
      if (evidenceResult.entries.length > 0 && !finalizeRetriedPastPost) {
        const evidenceEvents: JobAuditEvent[] = [];
        // Group entries by (file, pass). Pass association comes from the `review.pass` field on the
        // FileReview DB row (the FileReview record carries review.pass, not ParsedReviewComment — the
        // comment object has no `.pass` property). The grouping uses `review.file_path` to match
        // entries to their originating review unit.
        for (const review of reviews) {
          const reviewEntries = evidenceResult.entries.filter(
            (e) => e.path === review.file_path,
          );
          if (reviewEntries.length === 0) continue;
          const event = buildEvidenceHardDroppedEvent(review.file_path, review.pass, reviewEntries);
          if (event) evidenceEvents.push(event);
        }
        // REVIEWS FINDING #13 (Antigravity Suggestion): batch ALL evidence events into a single
        // appendJobAuditEvents call to preserve subrequest budget budget. Never emit per-(file, pass).
        try {
          await appendJobAuditEvents(env, job.id, evidenceEvents);
        } catch (error) {
          // Best-effort: log and continue (same posture as recordUnitAudit).
          logger.warn(`Failed to record evidence_hard_dropped events for job ${job.id}`, error);
        }
      }
    }
    ```

    This implementation:
    - Uses the persisted `finalizeRetriedPastPost` gate (CRITICAL PATH FIX #2)
    - Batches ALL evidence events into one `appendJobAuditEvents` call (FINDING #13)
    - Documents pass-grouping logic mapping `review.pass` from the DB row (FINDING #11)
    - Wraps the audit append in try/catch for best-effort posture

    Part B — Fix API shallow-merge in repos.ts (CRITICAL PATH FIX #3):

    In `src/server/routes/api/repos.ts` lines 163-166, the config PATCH handler does:
    ```
    const updatedParsedJson = {
      ...existing.parsedJson,
      ...configPatch,
    };
    ```

    This is a shallow merge. When `configPatch` contains `review: { evidence: { hard_drop: true } }`, the ENTIRE `review` object from `existing.parsedJson` is replaced, losing all other review settings (dedup, passes, rounds, etc.).

    Fix: deep-merge the `review` sub-object:
    ```
    const updatedParsedJson = {
      ...existing.parsedJson,
      ...configPatch,
      review: {
        ...existing.parsedJson.review,
        ...configPatch.review,
      },
    };
    ```

    This preserves all existing review sub-keys while allowing the patch to override specific review keys (like the new `evidence` block). If `configPatch.review` is undefined (model-only patch), the spread `...undefined` is a no-op, so existing review is preserved. The subsequent `repoConfigSchema.safeParse(updatedParsedJson)` at line 167 catches any remaining shape issues.
  </action>
  <acceptance_criteria>
    - When `evidence.hard_drop` is false (default), the code block is skipped and reviewedComments is unchanged (NREG-01)
    - When `evidence.hard_drop` is true, findings with absent/not_in_hunk existingCode are removed from reviewedComments
    - Exempt categories bypass the drop (case-insensitive)
    - `evidence_hard_dropped` audit events are batched into a single appendJobAuditEvents call
    - Gating uses finalizeRetriedPastPost (persisted job.steps check), not an in-memory variable — verified by code review
    - finalizeRetriedPastPost is computed once (moved before evidence block, duplicate at line 2410 removed)
    - repos.ts PATCH handler uses deep-merge: `review: { ...existing.parsedJson.review, ...configPatch.review }` instead of shallow spread
    - npm run build && npm run typecheck pass
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck; npm run build</automated>
  </verify>
  <done>Hard-drop gate compiles, is skipped when toggle is off; code review confirms: (a) finalizeRetriedPastPost used persistently, (b) audit events batched, (c) repos.ts uses deep-merge for review sub-object</done>
</task>

<task type="auto">
  <name>Task 2: Write integration test for evidence hard-drop in review-flow.spec.ts</name>
  <files>test/review-flow.spec.ts</files>
  <read_first>
    test/review-flow.spec.ts — read the first 50 lines and the last existing test case to understand test structure, helpers (createTestEnv, mockGitHub, etc.) and assertions pattern. Search for "evidence" to find existing evidence-related tests.
    src/server/core/evidence.ts — checkEvidence function API
    src/server/core/review.ts — finalize phase flow
  </read_first>
  <action>
    Add integration tests to `test/review-flow.spec.ts` for the evidence hard-drop gate. Follow the existing test patterns in this file (mock GitHub, use createTestEnv, etc.).

    **Test 1: evidence hard-drop removes findings with bad existingCode**
    - Set up a review job with `evidence.hard_drop: true` in configSnapshot
    - The configSnapshot MUST include the `evidence` key (FINDING #16 — integration test fixtures must include new config keys)
      ```
      configSnapshot: {
        ...defaultConfig,
        review: {
          ...defaultConfig.review,
          evidence: { hard_drop: true, hard_drop_exempt_categories: [] },
        },
      }
      ```
    - Configure mock model output where some findings have genuine `existing_code` (matching diff lines) and others have hallucinated `existing_code` (random text not in the diff)
    - Run the full review pipeline
    - Assert: evidence-invalid findings are NOT in the posted comments
    - Assert: audit events contain `evidence_hard_dropped` with correct droppedCount

    **Test 2: exempt categories bypass hard-drop**
    - Same setup as Test 1, but the hallucinated-evidence finding has category 'security'
    - configSnapshot sets `evidence: { hard_drop: true, hard_drop_exempt_categories: ['security'] }`
    - Run the full review pipeline
    - Assert: the security finding with bad evidence STILL posts
    - Assert: `evidence_hard_dropped` droppedCount excludes the exempt finding

    **Test 3: hard-drop disabled produces byte-identical output (NREG-01)**
    - Set up a job with evidence-invalid findings and `evidence.hard_drop: false` (default)
    - configSnapshot does NOT include `evidence` key (tests the default code path)
    - Run the review pipeline
    - Assert: all findings post (same as today's behavior)
    - Assert: no `evidence_hard_dropped` audit events emitted

    Test structure pattern (from existing tests):
    - Use `createTestEnv()` or equivalent test fixtures
    - Use `describe('evidence hard-drop', () => { ... })` to group tests
    - Use `it('should ...', async () => { ... })` for individual test cases
    - Use `expect(...)` for assertions
    - Check audit trail for `evidence_hard_dropped` events (job.audit array parsed from DB)

    For the model output mock, create a file review response where some findings have `existing_code` matching diff lines and others have hallucinated `existing_code` (text not in the diff). Ensure at least 3-5 findings total so the drop behavior is clearly distinguishable from the always-posts behavior.

    Check the existing test file for patterns like:
    - How configSnapshot overrides are passed to the review pipeline
    - How model responses are injected
    - How posted comments are retrieved for assertion
    - How audit events are read after pipeline completion
  </action>
  <acceptance_criteria>
    - Test 1 passes: evidence-invalid findings dropped when hard_drop=true
    - Test 2 passes: exempt categories (security) bypass drop
    - Test 3 passes: byte-identical output when hard_drop=false (NREG-01)
    - configSnapshot in Test 1/2 includes the `evidence` key explicitly
    - All tests pass as part of the full `npm test` suite (not flaky)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run test/review-flow.spec.ts -t "evidence hard-drop" -x</automated>
  </verify>
  <done>Three integration tests pass — evidence drop, exempt categories, and NREG-01 disabled mode</done>
</task>

<task type="auto">
  <name>Task 3: Add dashboard config UI for evidence section + minimal audit viewer renderer + lowercase normalization</name>
  <files>src/client/components/features/repos/review-settings-panel.tsx, src/client/lib/audit-grouping.ts, src/client/components/features/job-detail/audit-trail-viewer.tsx</files>
  <read_first>
    src/client/components/features/repos/review-settings-panel.tsx — Full file (728 lines). Study:
    - Lines 228-276: state declarations for all toggle blocks. Add evidence state vars here.
    - Lines 284-316: the `review` object assembly (immutable spreads). Add evidence spread here.
    - Lines 327-353: dirty detection. Add evidence fields to dirty comparison.
    - Lines 358-391: useEffect deps. Add evidence state vars to dependency array.
    - Lines 600-623: "Advanced" toggle section with all advanced toggles. Add evidence section here.
    - Lines 117-180: ListEditor component (used for exempt-categories list).
    - Lines 183-202: ToggleRow component (used for the hard_drop master toggle).

    src/client/lib/audit-grouping.ts lines 65-75 — normalizeAuditDisplayStage. Add evidence_hard_dropped mapping.

    src/client/components/features/job-detail/audit-trail-viewer.tsx lines 150-193 — evidence_missing_summary renderer case. Follow this exact pattern for the new evidence_hard_dropped renderer.
  </read_first>
  <action>
    ### Part A: Review Settings Panel (review-settings-panel.tsx)

    Add evidence section UI to the "Advanced" section of ReviewSettingsPanel. Cover ALL 4 integration points per REVIEWS.md CRITICAL PATH FIX #4:

    **1. State variables** (around line 276, after `escalateFloors`):
    ```
    const [evidenceHardDrop, setEvidenceHardDrop] = useState(current.evidence?.hard_drop ?? false);
    const [evidenceExemptCategories, setEvidenceExemptCategories] = useState<string[]>(
      current.evidence?.hard_drop_exempt_categories ?? ['security'],
    );
    ```

    **2. Review object spread** (around line 315, after `rounds` spread):
    ```
    evidence: {
      hard_drop: evidenceHardDrop,
      hard_drop_exempt_categories: evidenceExemptCategories,
    },
    ```

    **3. Dirty detection** (around line 353, after `escalateFloors`):
    ```
    evidenceHardDrop !== (current.evidence?.hard_drop ?? false) ||
    !orderedListEqual(evidenceExemptCategories, current.evidence?.hard_drop_exempt_categories ?? ['security']) ||
    ```

    **4. useEffect deps** (around line 388, after `escalateFloors`):
    Add `evidenceHardDrop` and `evidenceExemptCategories` to the deps array.

    **5. UI in Advanced section** (inside the `{advancedOpen && (` block, around line 722, after the escalate floors ToggleRow):
    ```
    <ToggleRow
      label="Evidence hard-drop"
      description="Drop findings whose existing_code evidence is not found in the hunk (hallucinated evidence)."
      checked={evidenceHardDrop}
      onCheckedChange={setEvidenceHardDrop}
      ariaLabel="Toggle evidence hard-drop"
    />
    <ListEditor
      title="Exempt categories"
      hint="Categories that always post findings regardless of evidence quality. Default: security."
      items={evidenceExemptCategories}
      onChange={(newItems) => {
        // REVIEWS FINDING #10: normalize exempt categories to lowercase on save to prevent silent
        // case-sensitivity mismatches between the config value (e.g., 'Security') and the
        // reviewCategories enum value ('security'). Lowercasing at the input boundary ensures the
        // checkEvidence comparison (which lowercases both sides) always matches.
        setEvidenceExemptCategories(newItems.map((item) => item.toLowerCase()));
      }}
      placeholder="e.g. correctness"
      addAriaLabel="New exempt category"
      emptyHint="All categories are subject to hard-drop. No exemptions."
    />
    ```

    **Important:** The onChange handler normalizes each item to lowercase immediately. This ensures that whatever the operator types (uppercase, mixed case) is stored as lowercase, matching the checkEvidence comparison strategy. The saved config value will always be lowercase.

    ### Part B: Audit grouping (audit-grouping.ts)

    In `normalizeAuditDisplayStage` (around line 73, after the `evidence_missing_summary` mapping), add:
    ```
    if (stage === 'evidence_hard_dropped') return 'evidence_missing';
    ```

    This maps the new event stage to the existing `evidence_missing` display group. The STAGE_ORDER entry at `'evidence_missing'` already exists (line 20). No change to STAGE_ORDER needed — `evidence_hard_dropped` and `evidence_missing_summary` both land in the same display group alongside legacy `evidence_missing` per-finding events. This satisfies REVIEWS FINDING #15 (OpenCode suggestion).

    ### Part C: Audit viewer renderer (audit-trail-viewer.tsx)

    In the `DecisionEvent` switch (around line 193, after the `evidence_missing_summary` case), add a new `evidence_hard_dropped` case:

    ```
    case 'evidence_hard_dropped':
      return (
        <li className="rounded-md border border-border/40 bg-card/40 p-3">
          <MetricLine label="file" value={event.file} />
          <MetricLine label="pass" value={event.pass} />
          <MetricLine label="dropped" value={String(event.droppedCount)} />
          {event.sample.length > 0 && (
            <ul className="mt-2 flex flex-col gap-1.5 border-t border-border/30 pt-2">
              {event.sample.map((s, i) => (
                <li key={i}>
                  <SampleIdentifier path={s.path} line={s.line} title={s.title} />
                  <div className="text-[10px] text-muted-foreground/70 font-mono">{s.reason}</div>
                </li>
              ))}
            </ul>
          )}
        </li>
      );
    ```

    The renderer follows the `evidence_missing_summary` case pattern exactly: file/pass as identifier, droppedCount as a metric line, and up to 20 sample entries with path/line/title and reason tag. The `default: return null` at line 347 already handles unknown stages — no additional work needed for fallback.
  </action>
  <acceptance_criteria>
    - ReviewSettingsPanel shows evidence hard-drop toggle in Advanced section
    - Toggle off = hard-drop disabled (default). Toggle on = hard-drop enabled.
    - Exempt categories list defaults to ['security']
    - ListEditor allows adding/removing exempt categories
    - ListEditor normalizes input to lowercase on save
    - The assembled review object includes `evidence: { hard_drop, hard_drop_exempt_categories }`
    - Dirty tracking detects changes to evidence settings
    - All 4 integration points present: state vars, spread, dirty detection, useEffect deps
    - npm run build passes (Vite client build)
    - audit-trail-viewer renders evidence_hard_dropped events with droppedCount and sample entries
    - STAGE_LABELS and audit-grouping map evidence_hard_dropped to evidence_missing display group
    - npm run typecheck passes
  </acceptance_criteria>
  <verify>
    <automated>npm run build; npm run typecheck</automated>
  </verify>
  <done>Dashboard config UI renders evidence section; ListEditor lowercases input; audit viewer renders evidence_hard_dropped events</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Config input → review pipeline | Config toggle controls whether evidence hard-drop runs; untrusted if config source is compromised |
| Config input → API PATCH route | The PATCH /api/repos/:owner/:repo/config handler writes user-provided config; shallow merge bug could corrupt review settings |
| Audit events → persistence | Sample entry titles must not leak content beyond privacy-bounded identifiers |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-01 | Tampering | review.ts evidence toggle gate | high | mitigate | `?? false` guard ensures hard-drop is off when config is missing/falsy; config default injection from Plan 26-01 Task 1 Part B ensures even old configs get defaults |
| T-26-03 | Tampering | core/evidence.ts exempt-category comparison | high | mitigate | Case-insensitive comparison enforced in checkEvidence (Plan 26-01 Task 2) |
| T-26-02 | Information Disclosure | audit-trail-viewer.tsx evidence_hard_dropped renderer | medium | mitigate | Title already redacted by redactFindingTitle at production time (audit.ts builder); renderer only displays the pre-redacted field |
| T-26-04 | Repudiation | review.ts audit emission | high | mitigate | At-most-once gate via persisted `finalizeRetriedPastPost` check (not in-memory variable that resets on Workflow retry — CRITICAL PATH FIX #2); same pattern as recordFinalizeDrops at line 2425 |
| T-26-06 | Tampering | repos.ts PATCH config handler shallow merge | high | mitigate | Deep-merge fix (Task 1 Part B): `review: { ...existing.parsedJson.review, ...configPatch.review }` prevents loss of review sub-keys when patching specific fields like evidence |
| T-26-05 | Denial of Service | review-settings-panel.tsx config editor | low | accept | Config is persisted via existing PATCH route with Zod validation; ListEditor normalizes input to lowercase; no new server-side risk |
| T-26-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages in this phase |

</threat_model>

<verification>
- npm run typecheck — tsc --noEmit passes (no type errors from evidence block, repos.ts deep-merge, new state vars, or renderer)
- npm run build — Vite + cf-typegen succeed (client build includes new evidence UI section)
- npx vitest run test/review-flow.spec.ts -x — integration test suite passes including new evidence hard-drop tests
- Manual confirmation: code review of insertion point (before dedup, after reviewedComments), finalizeRetriedPastPost usage (not in-memory gate), repos.ts deep-merge
</verification>

<success_criteria>
1. Evidence hard-drop gate compiles in review.ts and is skipped when toggle is off (NREG-01)
2. Gate runs before dedup, after reviewedComments resolution
3. Audit gate uses persisted finalizeRetriedPastPost (not in-memory variable) — CRITICAL PATH FIX #2
4. All evidence events batched into single appendJobAuditEvents call — FINDING #13
5. repos.ts PATCH handler uses deep-merge for review sub-object — CRITICAL PATH FIX #3
6. Three integration tests pass: drop behavior, exempt categories, NREG-01 disabled mode
7. Dashboard config UI shows evidence toggle + exempt categories list editor with lowercase normalization — CRITICAL PATH FIX #4
8. Audit viewer renders evidence_hard_dropped events with droppedCount and sample — FINDING #15
9. npm run build && npm run typecheck pass
</success_criteria>

<artifacts_this_phase_produces>
- review.ts: evidence hard-drop gate block in runFinalizePhase (gated on config.review.evidence?.hard_drop), at-most-once audit emission via persisted finalizeRetriedPastPost, batch appendJobAuditEvents call
- repos.ts: deep-merge fix `review: { ...existing.parsedJson.review, ...configPatch.review }` in PATCH handler
- review-flow.spec.ts: integration test group 'evidence hard-drop' with 3 test cases
- review-settings-panel.tsx: `evidenceHardDrop` + `evidenceExemptCategories` state, evidence section UI in Advanced panel with lowercase normalization in ListEditor onChange
- audit-grouping.ts: `evidence_hard_dropped` -> `evidence_missing` mapping in normalizeAuditDisplayStage
- audit-trail-viewer.tsx: `case 'evidence_hard_dropped':` renderer in DecisionEvent switch
</artifacts_this_phase_produces>

<output>
Create .planning/phases/26-evid-02-hard-drop-evidence-gate/26-02-SUMMARY.md when done
</output>
