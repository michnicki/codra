---
phase: 28-lrn-01-learned-rule-synthesis-from-reject-feedback
plan: 02
type: execute
wave: 2
depends_on:
  - 28-01
files_modified:
  - src/server/core/learned-rules.ts
  - src/server/core/review.ts
  - src/server/core/audit.ts
  - src/server/routes/api/repos.ts
  - test/learned-rules.spec.ts
  - test/review-flow.spec.ts
autonomous: true
requirements:
  - LRN-01
must_haves:
  truths:
    - clusterRejectFeedback groups rows by exact (finding_category, finding_file_path) and returns only clusters with >= 2 rejections
    - synthesizeRules produces new pending rules, skipping clusters whose (category, file_pattern) already exists in any status
    - suppressByLearnedRules drops findings matching active rules' (category, file_pattern) and returns kept + entries
    - Suppression runs AFTER EVID-02 hard-drop but BEFORE dedup in the finalize pipeline (D-14)
    - Each dropped finding produces a learned_rule_suppressed audit event (per (file, pass) aggregate, D-13)
    - POST /api/repos/:id/learned-rules/synthesize clusters reject_feedback and writes new pending rules to repo_configs.parsed_json
    - PATCH /api/repos/:id/learned-rules/:ruleId transitions rule status (pending->active, active->disabled, disabled->active)
    - All new behavior is gated on config.review.learning.enabled (NREG-01 — default off, byte-identical when disabled)
  artifacts:
    - src/server/core/learned-rules.ts
    - test/learned-rules.spec.ts
    - test/review-flow.spec.ts
  key_links:
    - learned-rules.ts suppressByLearnedRules -> review.ts finalize pipeline (after EVID-02, before dedup)
    - learned-rules.ts synthesizeRules -> repos.ts POST synthesize endpoint
    - audit.ts buildLearnedRuleSuppressedEvent -> jobAuditEventSchema learned_rule_suppressed variant
    - repos.ts PATCH rule status -> repo_configs.parsed_json.learning.learned_rules array update
---

<objective>
Implement the learned-rule synthesis algorithm, suppression logic, finalize pipeline wiring, audit event emission, and API endpoints for rule management.

Purpose: This plan delivers the core backend behavior — clustering rejections into candidate rules, suppressing findings matching active rules in finalize, and the API surface for the dashboard to manage rules.
Output: New core/learned-rules.ts module, suppression wiring in review.ts, audit event builder in audit.ts, API routes for synthesize + rule status, and comprehensive unit tests.
</objective>

<execution_context>
@/home/thomas/repos-ext/codra/.claude/gsd-core/workflows/execute-plan.md
@/home/thomas/repos-ext/codra/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-lrn-01-learned-rule-synthesis-from-reject-feedback/28-CONTEXT.md
@.planning/phases/28-lrn-01-learned-rule-synthesis-from-reject-feedback/28-RESEARCH.md
@.planning/phases/28-01-SUMMARY.md
@src/shared/schema.ts
@src/server/core/review.ts
@src/server/core/audit.ts
@src/server/core/evidence.ts
@src/server/db/repo-configs.ts
@src/server/routes/api/repos.ts
@test/schema-contract-inertness.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Core learned-rules.ts — clustering, synthesis, suppression + unit tests</name>
  <files>src/server/core/learned-rules.ts, test/learned-rules.spec.ts</files>
  <read_first>
    - src/shared/schema.ts (lines 118-277 for reviewConfigSchema learning shape; lines 830-1002 for jobAuditEventSchema)
    - src/server/core/evidence.ts (checkEvidence pattern — pure function, returns { kept, entries })
    - src/server/core/audit.ts (lines 664-691 for buildEvidenceHardDroppedEvent pattern)
    - src/server/core/review.ts (lines 2230-2266 for EVID-02 hard-drop pattern — the suppression template)
    - src/server/db/repo-configs.ts (loadRepoConfig, upsertRepoConfig, parseJsonColumn)
    - .planning/phases/28-lrn-01-learned-rule-synthesis-from-reject-feedback/28-CONTEXT.md (D-03 through D-08, D-12 through D-14)
    - .planning/phases/28-lrn-01-learned-rule-synthesis-from-reject-feedback/28-RESEARCH.md (code examples for clustering, synthesis, suppression)
  </read_first>
  <action>
    Create `src/server/core/learned-rules.ts` with three pure functions and one builder:

    1. `clusterRejectFeedback(rows)` — per D-03, D-04, D-05:
       - Input: array of `{ id: string; finding_category: string | null; finding_file_path: string | null }`
       - Group by exact `(finding_category, finding_file_path)` using a Map with key `${category}\0${filePath}`
       - Skip rows where `finding_category` or `finding_file_path` is null (D-02)
       - Filter to clusters with `ids.length >= 2` (D-04, hardcoded threshold)
       - Return array of `{ category: string; filePath: string; rejectionIds: string[] }`
       - Pure function, no I/O

    2. `synthesizeRules(clusters, existingRules)` — per D-06, D-05:
       - Input: clusters from step 1, existing rules array from `config.review.learning.learned_rules`
       - Deduplicate: build a Set of `${category}\0${file_pattern}` from existing rules (any status — pending, active, disabled)
       - For each cluster NOT in the existing set, produce a new rule: `{ id: crypto.randomUUID(), category, file_pattern: filePath, status: 'pending', source_rejection_ids: rejectionIds, created_at: new Date().toISOString() }`
       - Return the new rules array (caller writes to config)
       - Pure function, no I/O

    3. `suppressByLearnedRules(findings, activeRules)` — per D-12, D-14:
       - Input: `ParsedReviewComment[]` findings, active rules array (status === 'active')
       - For each finding, check if ANY active rule matches per D-07 (category + file_pattern only, no title matching, no severity threshold): `rule.category` case-insensitive equals `finding.category` AND `picomatch(rule.file_pattern, { dot: true })(finding.path)`. Use `picomatch` (already in deps, used in `core/diff.ts`) for glob matching so exact paths work as a subset.
       - Matching findings go to `entries` (for audit), non-matching go to `kept`
       - Entry shape: `{ path, line, title, matched_rule: rule.id }`
       - Return `{ kept: ParsedReviewComment[], entries: LearnedRuleSuppressionEntry[] }`
       - Pure function, no I/O. Import `picomatch` from 'picomatch'.

    4. Create `test/learned-rules.spec.ts` with unit tests covering:
       - Clustering: groups by (category, file_path), skips nulls, enforces min size 2
       - Synthesis: produces pending rules, skips duplicates (same category + file_pattern in any status), generates UUIDs
       - Suppression: drops matching findings (category + glob), keeps non-matching, produces correct entries, handles case-insensitive category match, handles empty active rules (no-op)
       - Edge cases: empty inputs, single rejection (no cluster), all null metadata

    Write RED tests first, then implement. Use `describe`/`it` blocks matching the function names.
  </action>
  <verify>
    <automated>npx vitest run test/learned-rules.spec.ts</automated>
  </verify>
  <behavior>
    - clusterRejectFeedback([{id:'1',finding_category:'quality',finding_file_path:'src/a.ts'},{id:'2',finding_category:'quality',finding_file_path:'src/a.ts'}]) returns 1 cluster with category='quality', filePath='src/a.ts', rejectionIds=['1','2']
    - clusterRejectFeedback with 1 row returns [] (min size 2)
    - clusterRejectFeedback with null category/file_path returns []
    - synthesizeRules(clusters, []) returns rules with status='pending'
    - synthesizeRules(clusters, [existing rule with same category+file_pattern]) returns [] (dedup)
    - suppressByLearnedRules(findings, [active rule matching category+path]) returns kept without the matching finding, entries with the dropped finding
    - suppressByLearnedRules(findings, []) returns all findings kept, empty entries
  </behavior>
  <acceptance_criteria>
    - src/server/core/learned-rules.ts exports clusterRejectFeedback, synthesizeRules, suppressByLearnedRules
    - test/learned-rules.spec.ts contains tests for all three functions with >10 test cases
    - `npx vitest run test/learned-rules.spec.ts` exits 0
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>
    Core learned-rules module with clustering, synthesis, and suppression functions. All unit tests pass. Functions are pure (no I/O).
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire suppression into finalize + audit event builder + API routes</name>
  <files>src/server/core/review.ts, src/server/core/audit.ts, src/server/routes/api/repos.ts</files>
  <read_first>
    - src/server/core/review.ts (lines 2230-2266 for EVID-02 hard-drop pattern — the suppression insertion point; lines 2088-2100 for finalize entry; lines 2268-2275 for mainReviews split)
    - src/server/core/audit.ts (lines 664-691 for buildEvidenceHardDroppedEvent — the pattern to follow for buildLearnedRuleSuppressedEvent)
    - src/server/core/learned-rules.ts (created in Task 1 — suppressByLearnedRules, synthesizeRules, clusterRejectFeedback)
    - src/server/routes/api/repos.ts (lines 140-195 for PATCH endpoint pattern, deep-merge of review sub-object)
    - src/server/db/repo-configs.ts (loadRepoConfig, upsertRepoConfig)
    - src/shared/schema.ts (learning config shape, learned_rule_suppressed audit event)
    - .planning/phases/28-lrn-01-learned-rule-synthesis-from-reject-feedback/28-CONTEXT.md (D-11, D-12, D-13, D-14)
    - .planning/phases/28-lrn-01-learned-rule-synthesis-from-reject-feedback/28-RESEARCH.md (Pattern 2 drop-before-dedup)
  </read_first>
  <action>
    1. Add `buildLearnedRuleSuppressedEvent` to `audit.ts`:
       - Follow `buildEvidenceHardDroppedEvent` (line 664) exactly.
       - Signature: `buildLearnedRuleSuppressedEvent(file: string, pass: FileReviewPass, entries: LearnedRuleSuppressionEntry[]): JobAuditEvent | null`
       - Return null if entries is empty.
       - Cap sample at 20 entries (use a constant `LEARNED_RULE_SUPPRESS_SAMPLE_CAP = 20`).
       - Map entries to sample: `{ path, line, title: redactFindingTitle(entry.title), matched_rule: entry.matched_rule }`.
       - Build event: `{ stage: 'learned_rule_suppressed', file, pass, droppedCount: entries.length, sample, timestamp: new Date().toISOString() }`.
       - Return `event satisfies JobAuditEvent` (never cast `as`).
       - Define `LearnedRuleSuppressedEntry` type: `{ path: string; line: number | null; title: string; matched_rule: string }` (export it from audit.ts or learned-rules.ts).

    2. Wire suppression into `review.ts` finalize pipeline (per D-14):
       - Insert the learned-rule suppression block AFTER the EVID-02 hard-drop block (line 2266) and BEFORE the `mainReviews` split (line 2268).
       - Pattern (follows EVID-02 exactly):
         ```
         if (config.review.learning?.enabled ?? false) {
           const activeRules = (config.review.learning?.learned_rules ?? []).filter(r => r.status === 'active');
           if (activeRules.length > 0) {
             const lrResult = suppressByLearnedRules(reviewedComments, activeRules);
             reviewedComments = lrResult.kept;
             if (lrResult.entries.length > 0 && !finalizeRetriedPastPost) {
               const lrEvents: JobAuditEvent[] = [];
               for (const review of reviews) {
                 const reviewEntries = lrResult.entries.filter(e => e.path === review.file_path);
                 if (reviewEntries.length === 0) continue;
                 const event = buildLearnedRuleSuppressedEvent(review.file_path, review.pass, reviewEntries);
                 if (event) lrEvents.push(event);
               }
               try { await appendJobAuditEvents(env, job.id, lrEvents); } catch (error) { logger.warn(...); }
             }
           }
         }
         ```
       - Import `suppressByLearnedRules` from `@server/core/learned-rules` and `buildLearnedRuleSuppressedEvent` from `@server/core/audit`.
       - The `finalizeRetriedPastPost` gate ensures at-most-once audit emission (same pattern as EVID-02).

    3. Add API endpoints to `routes/api/repos.ts`:
       - `POST /api/repos/:id/learned-rules/synthesize` — per D-11:
         - Require `requireSession` + `requireCsrfHeader` middleware (same as existing routes).
         - Load repo config via `loadRepoConfig`.
         - Check `config.review.learning?.enabled` — if false, return 400 with error message.
         - Query `reject_feedback` for this repo (JOIN repositories to get repo id, filter by vcs_provider + workspace + repo_slug).
         - Call `clusterRejectFeedback(rows)` then `synthesizeRules(clusters, config.review.learning.learned_rules)`.
         - If new rules are empty, return `{ ok: true, rules: [], message: 'No new rule candidates found' }`.
         - Otherwise, append new rules to `config.review.learning.learned_rules`, upsert the config via `upsertRepoConfig`.
         - Return `{ ok: true, rules: newRules }`.

       - `PATCH /api/repos/:id/learned-rules/:ruleId` — per D-08:
         - Require `requireSession` + `requireCsrfHeader`.
         - Parse request body: `{ status: 'active' | 'disabled' | 'pending' }`.
         - Validate status transitions: pending→active, active→disabled, disabled→active (reject invalid transitions with 400).
         - Load repo config, find the rule by id in `config.review.learning.learned_rules`.
         - If rule not found, return 404.
         - Update the rule's status, upsert config.
         - Return `{ ok: true, rule: updatedRule }`.

       - Both endpoints need the repo id resolution (existing pattern in repos.ts uses `getRepoConfig` or similar).
       - Add a helper query in `reject-feedback.ts` or a new DB module: `getRejectFeedbackForRepo(env, { vcsProvider, workspace, repoSlug })` that returns all reject_feedback rows for the repo (used by the synthesize endpoint).

    4. Add the `getRejectFeedbackForRepo` query function to `reject-feedback.ts`:
       - Query: `SELECT * FROM reject_feedback WHERE vcs_provider = $1 AND workspace = $2 AND repo_slug = $3 ORDER BY created_at DESC`
       - Returns `RejectFeedbackRow[]`.
  </action>
  <verify>
    <automated>npx vitest run test/learned-rules.spec.ts && npx vitest run test/schema-contract-inertness.spec.ts && npx vitest run test/review-flow.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/server/core/audit.ts exports buildLearnedRuleSuppressedEvent
    - src/server/core/review.ts contains learned-rule suppression block between EVID-02 hard-drop and mainReviews split
    - The suppression block is gated on `config.review.learning?.enabled ?? false`
    - The suppression block calls suppressByLearnedRules and emits audit events via appendJobAuditEvents
    - src/server/routes/api/repos.ts has POST /api/repos/:id/learned-rules/synthesize endpoint
    - src/server/routes/api/repos.ts has PATCH /api/repos/:id/learned-rules/:ruleId endpoint
    - Both endpoints require session + CSRF
    - src/server/db/reject-feedback.ts exports getRejectFeedbackForRepo
    - `npx vitest run test/learned-rules.spec.ts` exits 0
    - `npx vitest run test/review-flow.spec.ts` exits 0
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>
    Suppression wired into finalize after EVID-02, before dedup. Audit events emitted per (file, pass). API endpoints for synthesize and rule status management. All tests pass including integration tests.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integration tests — suppression in finalize pipeline</name>
  <files>test/review-flow.spec.ts</files>
  <read_first>
    - test/review-flow.spec.ts (existing integration test patterns — how jobs are created, run through finalize, and assertions on posted findings)
    - src/server/core/review.ts (finalize pipeline — where suppression is wired from Task 2)
    - src/server/core/learned-rules.ts (suppressByLearnedRules, the function under integration test)
    - .planning/phases/28-lrn-01-learned-rule-synthesis-from-reject-feedback/28-VALIDATION.md (28-03-03: config gating integration test requirement)
    - .planning/phases/28-lrn-01-learned-rule-synthesis-from-reject-feedback/28-RESEARCH.md (Phase Requirements -> Test Map: integration test for config gating)
  </read_first>
  <action>
    Add integration test cases to `test/review-flow.spec.ts` that exercise the learned-rule suppression path through the full finalize pipeline. The existing test file already has patterns for creating jobs, running them through review phases, and asserting on posted findings. Add a new `describe('learned rule suppression')` block with:

    1. **Test: active learned rule suppresses matching finding in finalize.** Setup: create a repo config with `learning.enabled: true` and one active rule with `category: 'quality'` and `file_pattern` matching a test file path. Create a job with a review comment whose `category` matches the rule. Run the job through finalize. Assert: the matching finding is NOT in the posted output (was suppressed), and a `learned_rule_suppressed` audit event exists in the job's audit events.

    2. **Test: learning.enabled=false does not suppress findings.** Setup: same repo config but with `learning.enabled: false`. Run the same job through finalize. Assert: ALL findings appear in the posted output (no suppression), and no `learned_rule_suppressed` audit events exist.

    3. **Test: no active rules (all pending/disabled) does not suppress.** Setup: `learning.enabled: true` but the rule has `status: 'pending'`. Assert: findings are not suppressed.

    Use the existing test infrastructure in review-flow.spec.ts for job setup, mock provider, and assertion helpers. Follow the established test patterns (e.g., how EVID-02 hard-drop tests are structured if they exist). If the test file does not yet have EVID-02 integration tests, follow the general job-lifecycle test pattern and create the suppression test as a standalone block.

    Write RED tests first (they will fail because the finalize wiring from Task 2 is not yet committed in the test context), then verify they pass after Task 2 is merged.
  </action>
  <verify>
    <automated>npx vitest run test/review-flow.spec.ts</automated>
  </verify>
  <behavior>
    - Job with active learned rule + matching finding: finding is suppressed, audit event emitted
    - Job with learning.enabled=false: finding is NOT suppressed, no audit event
    - Job with pending rule only: finding is NOT suppressed
  </behavior>
  <acceptance_criteria>
    - test/review-flow.spec.ts contains describe block 'learned rule suppression' with at least 3 test cases
    - Tests cover: active rule suppresses, disabled config does not suppress, pending rule does not suppress
    - Tests assert on audit event presence/absence
    - `npx vitest run test/review-flow.spec.ts` exits 0
  </acceptance_criteria>
  <done>
    Integration tests in review-flow.spec.ts verify end-to-end suppression behavior: active rules suppress findings in finalize, disabled config skips suppression, and audit events are emitted correctly.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dashboard -> API endpoints | Synthesize and rule-status endpoints are authenticated (session + CSRF) |
| API -> repo_configs.parsed_json | Rule data written to config JSONB must be validated by repoConfigSchema |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-03 | Tampering | POST synthesize endpoint | medium | mitigate | Endpoint requires session + CSRF. Config write validated by repoConfigSchema.safeParse(). Reject_feedback rows are only created by authenticated reject commands. |
| T-28-04 | Elevation | PATCH rule status endpoint | medium | mitigate | Endpoint requires session + CSRF. Status transitions validated (pending->active, active->disabled, disabled->active only). Rule ID must exist in config. |
| T-28-05 | Denial of Service | Synthesis on large reject_feedback | low | accept | Synthesis is on-demand (D-10), not automatic. Clustering is O(n) on reject_feedback rows. Bounded by repo's rejection history. |
| T-28-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages installed (zero new deps constraint). |
</threat_model>

<verification>
- `npx vitest run test/learned-rules.spec.ts` — unit tests for clustering, synthesis, suppression
- `npx vitest run test/review-flow.spec.ts` — integration tests for suppression in finalize pipeline
- `npx vitest run test/schema-contract-inertness.spec.ts` — NREG-01 preserved
- `npm run typecheck` — no type errors
</verification>

<success_criteria>
- Clustering, synthesis, and suppression functions work correctly (unit tests pass)
- Suppression is wired into finalize after EVID-02, before dedup
- Audit events are emitted for suppressed findings
- API endpoints for synthesize and rule status management are functional
- All behavior is gated on learning.enabled (NREG-01 preserved)
- Integration tests verify end-to-end suppression: active rules suppress, disabled config skips, pending rules do not suppress
</success_criteria>

<output>
Create `.planning/phases/28-lrn-01-learned-rule-synthesis-from-reject-feedback/28-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Symbol | Type | File | Description |
|--------|------|------|-------------|
| `clusterRejectFeedback` | Function | src/server/core/learned-rules.ts | Groups reject_feedback rows by (category, file_path), min size 2 |
| `synthesizeRules` | Function | src/server/core/learned-rules.ts | Produces new pending rules from clusters, skipping duplicates |
| `suppressByLearnedRules` | Function | src/server/core/learned-rules.ts | Drops findings matching active rules, returns kept + entries |
| `buildLearnedRuleSuppressedEvent` | Function | src/server/core/audit.ts | Builds per-(file, pass) aggregate audit event |
| `LearnedRuleSuppressedEntry` | Type | src/server/core/audit.ts or learned-rules.ts | Entry shape for suppression audit |
| `getRejectFeedbackForRepo` | Function | src/server/db/reject-feedback.ts | Queries all reject_feedback for a repo |
| `POST /api/repos/:id/learned-rules/synthesize` | API endpoint | src/server/routes/api/repos.ts | On-demand synthesis trigger |
| `PATCH /api/repos/:id/learned-rules/:ruleId` | API endpoint | src/server/routes/api/repos.ts | Rule status transition |
| Integration tests | Test file | test/review-flow.spec.ts | End-to-end suppression: active rule suppresses, disabled config skips, pending rule does not suppress |
