---
phase: 28-lrn-01-learned-rule-synthesis-from-reject-feedback
plan: 03
type: execute
wave: 3
depends_on:
  - 28-02
files_modified:
  - src/client/components/features/repos/learned-rules-panel.tsx
  - src/client/components/features/repos/rule-card.tsx
  - src/client/pages/repos.tsx
  - src/client/lib/audit-grouping.ts
  - src/client/lib/review-config-draft.ts
autonomous: true
requirements:
  - LRN-01
must_haves:
  truths:
    - LearnedRulesPanel renders a toggle (learning.enabled), synthesize button, and rule list
    - RuleCard shows category, file_pattern, rejection count, status badge, and action buttons
    - Rules are grouped by status: pending first, then active, then disabled
    - Approve button transitions pending->active, Disable transitions active->disabled, Re-enable transitions disabled->active
    - Synthesize button fires POST /api/repos/:id/learned-rules/synthesize
    - learning.toggle is part of the review config PATCH (dirty-tracking, Apply flow)
    - learned_rule_suppressed audit stage is normalized in STAGE_ORDER for the audit trail viewer
    - Empty states render when feature is off or no rules exist
  artifacts:
    - src/client/components/features/repos/learned-rules-panel.tsx
    - src/client/components/features/repos/rule-card.tsx
  key_links:
    - learned-rules-panel.tsx -> PATCH /api/repos/:id/config (learning toggle via existing config flow)
    - learned-rules-panel.tsx -> POST /api/repos/:id/learned-rules/synthesize (synthesize button)
    - rule-card.tsx -> PATCH /api/repos/:id/learned-rules/:ruleId (approve/disable/re-enable)
    - audit-grouping.ts STAGE_ORDER -> learned_rule_suppressed normalization
---

<objective>
Build the dashboard UI for learned rules: a LearnedRulesPanel component (toggle + synthesize button + rule list), a RuleCard component (individual rule display with status transitions), integration into the repo config page, and audit stage normalization for the trail viewer.

Purpose: Operators need a way to review, approve, and manage synthesized suppression rules before they take effect. This plan delivers the complete dashboard surface.
Output: Two new React components, wiring into the repo config modal, and audit stage normalization.
</objective>

<execution_context>
@/home/thomas/repos-ext/codra/.claude/gsd-core/workflows/execute-plan.md
@/home/thomas/repos-ext/codra/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-lrn-01-learned-rule-synthesis-from-reject-feedback/28-CONTEXT.md
@.planning/phases/28-lrn-01-learned-rule-synthesis-from-reject-feedback/28-RESEARCH.md
@.planning/phases/28-lrn-01-learned-rule-synthesis-from-reject-feedback/28-UI-SPEC.md
@.planning/phases/28-01-SUMMARY.md
@.planning/phases/28-02-SUMMARY.md
@src/client/pages/repos.tsx
@src/client/lib/review-config-draft.ts
@src/client/lib/audit-grouping.ts
@src/client/components/features/repos/review-settings-panel.tsx
@src/client/components/ui/button.tsx
@src/client/components/ui/badge.tsx
@src/client/components/ui/switch.tsx
@src/client/components/ui/card.tsx
@src/client/components/ui/confirm-dialog.tsx
@src/client/components/ui/alert.tsx
@src/shared/schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: LearnedRulesPanel + RuleCard components</name>
  <files>src/client/components/features/repos/learned-rules-panel.tsx, src/client/components/features/repos/rule-card.tsx</files>
  <read_first>
    - src/client/components/features/repos/review-settings-panel.tsx (ToggleRow pattern, dirty-tracking, draft state)
    - src/client/components/ui/button.tsx (variant="default", variant="destructive-outline", size="sm")
    - src/client/components/ui/badge.tsx (variant="info", "success", "neutral")
    - src/client/components/ui/switch.tsx (toggle component)
    - src/client/components/ui/card.tsx (surface card container)
    - src/client/components/ui/confirm-dialog.tsx (destructive confirmation)
    - src/client/components/ui/alert.tsx (error display)
    - src/client/lib/review-config-draft.ts (ReviewSettingsDraft interface, mergeReviewPatch)
    - src/shared/schema.ts (learning config shape — learned_rules array, rule status enum)
    - .planning/phases/28-lrn-01-learned-rule-synthesis-from-reject-feedback/28-UI-SPEC.md (full component inventory, layout, interaction contracts, copywriting, empty states)
    - .planning/phases/28-lrn-01-learned-rule-synthesis-from-reject-feedback/28-CONTEXT.md (D-15 for section layout)
  </read_first>
  <action>
    1. Create `src/client/components/features/repos/rule-card.tsx`:
       - Export `RuleCard` component. Props: `{ rule: LearnedRule; onApprove: (id: string) => void; onDisable: (id: string) => void; onReEnable: (id: string) => void; actionLoading: string | null }`
       - `LearnedRule` type: `{ id: string; category: string; file_pattern: string; status: 'pending' | 'active' | 'disabled'; source_rejection_ids: string[]; created_at: string }` — matches the Zod schema shape.
       - Layout per UI-SPEC RuleCard Layout:
         - Top row: Status badge (left) + category label (right of badge). Badge variants: pending=`info`, active=`success`, disabled=`neutral`. Use the existing `Badge` component from `components/ui/badge.tsx`.
         - Second row: `file_pattern` in `font-mono text-xs text-muted-foreground break-all`.
         - Bottom row: rejection count (`text-xs text-muted-foreground`: "N rejections") left-aligned, action buttons right-aligned.
       - Action buttons per UI-SPEC:
         - Pending rule: "Approve rule" button (`variant="default" size="sm"`, `aria-label="Approve rule"`)
         - Active rule: "Disable rule" button (`variant="destructive-outline" size="sm"`, `aria-label="Disable rule"`)
         - Disabled rule: "Re-enable rule" button (`variant="default" size="sm"`, `aria-label="Re-enable rule"`)
       - Disable action requires confirmation via `ConfirmDialog` (title="Disable rule", description from UI-SPEC copy).
       - Button disabled state when `actionLoading === rule.id` (optimistic loading).
       - Card uses `surface` utility class (bg-card border border-border rounded-xl shadow-md p-4).

    2. Create `src/client/components/features/repos/learned-rules-panel.tsx`:
       - Export `LearnedRulesPanel` component. Props: `{ config: RepoConfig['review']['learning']; onLearningChange: (learning: RepoConfig['review']['learning']) => void; onSynthesized: () => void; repoId: string }` — `onSynthesized` triggers parent config refresh after synthesis or rule status change (C7)
       - Section heading: "Learned Rules" (`text-base font-semibold`).
       - ToggleRow: "Learned rules" label + Switch + description from UI-SPEC copy. Follow `review-settings-panel.tsx` ToggleRow pattern. Toggle updates `config.enabled` and calls `onLearningChange`.
       - When `config.enabled` is false: render empty state (feature off) per UI-SPEC — icon (BrainOff or LightbulbOff from lucide-react), heading "Learned rules are disabled", body text.
       - When `config.enabled` is true:
         - "Synthesize rules" button (`variant="default"`, top-right of section). Fires `POST /api/repos/${repoId}/learned-rules/synthesize`. Loading state: disabled + spinner. Success: toast "Rules synthesized. Review pending rules below." Empty result: toast from UI-SPEC. Error: inline Alert.
         - Rule list: group rules by status (pending first, then active, then disabled). Render `RuleCard` for each.
         - Empty state (no rules): icon (Sparkles from lucide-react), heading "No learned rules yet", body text, "Synthesize rules" button.
       - Optimistic updates: when approve/disable/re-enable succeeds, update the local rule list immediately. On error, toast + revert.
       - **[C7 — BLOCKING] Config refresh after synthesis:** When the "Synthesize rules" API call succeeds, do NOT attempt to merge the response into the client draft. Instead, call a `onSynthesized` callback (passed as prop) that triggers the parent modal to re-fetch the full config from the server via `loadRepoConfig`. This avoids the stale-draft-overwrites-rules problem where unsaved client changes would overwrite newly synthesized rules if the operator clicks Apply later. The `onSynthesized` callback is separate from `onLearningChange` (which handles the toggle).
       - State management: `synthesizing` boolean, `actionLoading` string | null (rule ID being acted on), `error` string | null.
       - Fetch rules from config (passed as prop — the parent modal loads config). After rule status change (approve/disable/re-enable), also trigger a config refresh from the parent via `onSynthesized` callback.
       - Use `sonner` toast (already integrated) for success/error notifications per UI-SPEC copywriting.

    3. Verify both components compile: `npm run typecheck`.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - src/client/components/features/repos/rule-card.tsx exports RuleCard component
    - src/client/components/features/repos/learned-rules-panel.tsx exports LearnedRulesPanel component
    - RuleCard renders status badge, category, file_pattern, rejection count, and action buttons
    - LearnedRulesPanel renders toggle, synthesize button, and rule list (or empty state)
    - Disable action uses ConfirmDialog for confirmation
    - LearnedRulesPanel accepts `onSynthesized` prop and calls it after synthesis or rule status change to trigger server config refresh (C7)
    - All aria-labels present per UI-SPEC accessibility section
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>
    LearnedRulesPanel and RuleCard components compile and follow the UI-SPEC layout, copywriting, and interaction contracts. **[C9] UI test gap acknowledged:** Component runtime tests require browser test environment (nix-shell per MEMORY.md). Verification relies on: (1) typecheck, (2) manual browser check at end-of-phase, (3) backend integration tests in Plan 02 covering the API endpoints the UI calls.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire into repo config page + audit stage normalization</name>
  <files>src/client/pages/repos.tsx, src/client/lib/audit-grouping.ts, src/client/lib/review-config-draft.ts</files>
  <read_first>
    - src/client/pages/repos.tsx (lines 440-546 for RepoModelModal, handleApply, mergeReviewPatch; lines 600-640 for ReviewSettingsPanel integration)
    - src/client/lib/review-config-draft.ts (mergeReviewPatch, ReviewSettingsDraft)
    - src/client/lib/audit-grouping.ts (STAGE_ORDER, normalizeAuditDisplayStage)
    - src/client/components/features/repos/learned-rules-panel.tsx (created in Task 1)
    - src/shared/schema.ts (learning config shape)
    - .planning/phases/28-lrn-01-learned-rule-synthesis-from-reject-feedback/28-UI-SPEC.md (page layout section)
  </read_first>
  <action>
    1. Wire `LearnedRulesPanel` into the repo config page (`repos.tsx`):
       - Import `LearnedRulesPanel` in `repos.tsx`.
       - Add `learningDraft` state to `RepoModelModal` (similar to `reviewSettingsDraft`): tracks the current `learning` config, dirty flag, and valid flag.
       - Initialize `learningDraft` from `repo.parsedJson.review.learning` when the modal opens (in the `useEffect` that resets state on repo change).
       - Add `learningDirty` to the dirty tracking: `const learningDirty = learningDraft?.dirty ?? false;`. Update `dirty` to include it: `const dirty = modelDirty || interactiveDirty || reviewSettingsDirty || learningDirty;`.
       - In `handleApply`, include the learning config in the PATCH. The `mergeReviewPatch` function in `review-config-draft.ts` already spreads `settingsFields` over `currentReview`, so pass `learning` as part of `settingsFields` when learning changed. Specifically: if `learningDirty`, include `learning: learningDraft.learning` in the `settingsFields` partial passed to `mergeReviewPatch`.
       - Render `LearnedRulesPanel` below `ReviewSettingsPanel` inside the modal's review settings section. Pass `config={learningDraft?.learning ?? repo?.parsedJson.review.learning}`, `onLearningChange` callback that updates `learningDraft`, `onSynthesized` callback that re-fetches config from server (C7), and `repoId={selectedRepoId}`.
       - The `onSynthesized` callback: re-fetches the repo config from the server (via the existing `loadRepoConfig` API call pattern in repos.ts) and updates the local state with fresh data. This ensures that after synthesis or rule status changes, the UI reflects the server-truth, not a potentially stale client draft.
       - The "Synthesize rules" button in `LearnedRulesPanel` fires its own API call (not through the parent's Apply flow). The learning toggle is part of the Apply flow (config PATCH). These are separate concerns.

    2. Update `mergeReviewPatch` in `review-config-draft.ts` if needed:
       - Verify that the existing `mergeReviewPatch` function handles the new `learning` key correctly. Since it does `{ ...currentReview, ...(settingsFields ?? {}), ...(interactive ? { interactive } : {}) }`, a `learning` key in `settingsFields` will be spread correctly. No change needed to `mergeReviewPatch` itself.
       - But verify: if `settingsFields` includes `learning`, it should override `currentReview.learning`. If `settingsFields` doesn't include `learning`, the spread preserves `currentReview.learning`. This is correct behavior.

    3. Add `learned_rule_suppressed` to audit stage normalization (`audit-grouping.ts`):
       - Add `'learned_rule_suppressed'` to the `STAGE_ORDER` array. Place it after `'evidence_hard_dropped'` and before the rounds stages, since learned-rule suppression runs in the same finalize phase as evidence hard-drop.
       - Update `normalizeAuditDisplayStage` to map `'learned_rule_suppressed'` to the `'evidence'` display group (same as `evidence_hard_dropped` and `evidence_missing`). This groups all evidence/suppression events together in the audit trail viewer.
       - The mapping: `if (stage === 'learned_rule_suppressed') return 'evidence';` (or whatever display group name is used — check the existing mappings for `evidence_hard_dropped`).

    4. Verify compilation and NREG-01: `npm run typecheck`. The dashboard changes are client-only and don't affect NREG-01 byte-identity.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - src/client/pages/repos.tsx imports and renders LearnedRulesPanel below ReviewSettingsPanel
    - learning toggle is tracked as dirty and included in the Apply PATCH flow
    - `onSynthesized` callback in repos.tsx triggers config re-fetch from server (C7)
    - learned_rule_suppressed is in STAGE_ORDER in audit-grouping.ts
    - normalizeAuditDisplayStage maps learned_rule_suppressed to the evidence display group
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>
    LearnedRulesPanel integrated into repo config modal. Learning toggle participates in dirty-tracking and Apply flow. Audit stage normalization includes learned_rule_suppressed. All typechecks pass. NOTE: Runtime behavior (toggle interaction, synthesize button, audit stage rendering) requires browser test environment (nix-shell per MEMORY.md) and is verified at the end-of-phase human checkpoint.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dashboard -> API | Client fires POST synthesize and PATCH rule status via authenticated API calls |
| Config display -> config write | Learning toggle is part of the existing config PATCH flow (session + CSRF protected) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-06 | Tampering | Client-side rule status transitions | low | accept | All transitions go through authenticated PATCH endpoint (server validates). Client-side optimistic updates are cosmetic only. |
| T-28-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages installed. Existing lucide-react, sonner, Radix UI only. |
</threat_model>

<verification>
- `npm run typecheck` — no type errors
- Manual: navigate to repo config page, verify Learned Rules section renders with toggle, synthesize button, and empty state
- **[C9] UI test gap acknowledged:** Component runtime tests (@testing-library/react) are not included in this plan because the vitest browser project requires nix-shell for Chromium dependencies (per MEMORY.md). This is a known environment constraint, not a coverage oversight. Wave 3 is verified by: (1) `npm run typecheck` confirming type safety, (2) manual browser verification at the end-of-phase human checkpoint, and (3) the backend integration tests in Plan 02 covering the API endpoints the UI calls. If browser infrastructure becomes available, render tests for LearnedRulesPanel and RuleCard should be added as a follow-up.
</verification>

<success_criteria>
- LearnedRulesPanel renders in repo config modal with toggle, synthesize button, and rule list
- RuleCard shows category, file_pattern, rejection count, status badge, and action buttons
- Learning toggle participates in dirty-tracking and config PATCH
- Audit trail viewer groups learned_rule_suppressed events with evidence events
- All typechecks pass
</success_criteria>

<output>
Create `.planning/phases/28-lrn-01-learned-rule-synthesis-from-reject-feedback/28-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Symbol | Type | File | Description |
|--------|------|------|-------------|
| `LearnedRulesPanel` | React component | src/client/components/features/repos/learned-rules-panel.tsx | Toggle + synthesize button + rule list section |
| `RuleCard` | React component | src/client/components/features/repos/rule-card.tsx | Individual rule display with status transitions |
| `learned_rule_suppressed` in STAGE_ORDER | Audit normalization | src/client/lib/audit-grouping.ts | Stage mapped to evidence display group |
