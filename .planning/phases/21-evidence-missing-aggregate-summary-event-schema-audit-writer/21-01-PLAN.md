---
phase: 21-evidence-missing-aggregate-summary-event-schema-audit-writer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/schema.ts
  - src/server/core/audit.ts
  - test/audit-trail.spec.ts
autonomous: true
requirements:
  - EVID-03
user_setup: []
must_haves:
  truths:
    - The jobAuditEventSchema discriminated union accepts a new evidence_missing_summary stage literal with stage, file, pass, absentCount, notInHunkCount, sample[{path,line,title,reason}], and timestamp (in that order, timestamp last per existing convention)
    - The legacy evidence_missing per-finding variant remains byte-identical so old persisted events still parse
    - buildEvidenceMissingSummary returns a single aggregate event with correct counts when given entries, or null when given empty input (D-05)
    - Sample entries preserve model-emission order (D-07), titles are redacted via redactFindingTitle, sample is bounded to max 20 via EVIDENCE_MISSING_SAMPLE_CAP constant
    - The EvidenceMissingEntry type is exported from audit.ts; the builder return type is EvidenceMissingSummaryAuditEvent | null (using satisfies JobAuditEvent, never as JobAuditEvent per Codex REVIEWS finding)
    - Builder output round-trips through jobAuditEventSchema without error (schema round-trip test added per Codex REVIEWS finding)
  artifacts:
    - schema.ts: evidence_missing_summary arm in jobAuditEventSchema (timestamp last, .passthrough())
    - audit.ts: EvidenceMissingEntry interface, EvidenceMissingSummaryAuditEvent type, EVIDENCE_MISSING_SAMPLE_CAP constant (near line 347 area with other caps), buildEvidenceMissingSummary builder function
    - audit-trail.spec.ts: describe('buildEvidenceMissingSummary') block with unit tests including schema round-trip
  key_links:
    - Builder output flows into severityAuditEvents (not a new channel) -> recordUnitAudit unchanged
    - Sample titles pass through redactFindingTitle before storage (privacy boundary, T-15-04-01)
    - Builder result satisfies JobAuditEvent (validated by schema round-trip in tests, not by runtime cast)
---

<objective>
Add the `evidence_missing_summary` schema variant to `jobAuditEventSchema` and implement the pure builder `buildEvidenceMissingSummary` in `audit.ts`, following the `buildFileSkipEvents` / `buildEnsembleVoteAuditEvent` precedent exactly.

Purpose: Replace N per-finding `evidence_missing` audit events with ONE bounded aggregate per `(file, pass)` unit, preventing a non-compliant model's flood of events from evicting `drafted`/`severity_adjusted` telemetry from the 500-event ring buffer.

Output: Schema variant in `jobAuditEventSchema`, exported `EvidenceMissingEntry` interface, `EvidenceMissingSummaryAuditEvent` type, `EVIDENCE_MISSING_SAMPLE_CAP` constant, `buildEvidenceMissingSummary` builder function returning type-safe aggregate via `satisfies JobAuditEvent`, builder unit tests with schema round-trip validation.
</objective>

<execution_context>
@/home/thomas/repos-ext/codra/.claude/gsd-core/workflows/execute-plan.md
@/home/thomas/repos-ext/codra/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-evidence-missing-aggregate-summary-event-schema-audit-writer/21-CONTEXT.md
@.planning/phases/21-evidence-missing-aggregate-summary-event-schema-audit-writer/21-RESEARCH.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-evidence-missing-aggregate-summary-event-schema-audit-writer/21-REVIEWS.md (all actionable findings incorporated per disposition below)

# Source files (read with specific line ranges for context)
@src/shared/schema.ts (lines 795-920 — full jobAuditEventSchema, existing evidence_missing variant at 907-920)
@src/server/core/audit.ts (lines 1-8 imports, 228-257 buildFileSkipEvents precedent, 340-413 ensemble sample caps + builder, 451-490 critic caps + builder, 531-565 walkthrough enrichment builder)
@src/server/core/audit-redact.ts (lines 53-56 redactFindingTitle)
@test/audit-trail.spec.ts (lines 1-9 imports, 228-295 buildFileSkipEvents describe block)
</context>

## Review Findings Incorporated

| Review Source | Finding | Disposition | Where Addressed |
|---------------|---------|-------------|-----------------|
| Codex | Ad-hoc `node -e` verification commands are false-green | Incorporated | All verify blocks in this plan use bare Vitest commands, not `node -e` with pipe masking |
| Codex | `as JobAuditEvent` cast conceals malformed output; builder tests don't schema-roundtrip | Incorporated | Task 2 uses `satisfies JobAuditEvent`; Task 3 adds schema round-trip test case per Codex suggestion |
| Codex | Missing schema tests for new variant (valid, invalid, legacy) | Incorporated | Task 1 verification creates a temporary scaffold test that validates valid/invalid/legacy/overflow shapes |
| OpenCode | Sample cap constant should be near line ~347 (near ENSEMBLE_WINNING_SAMPLE_CAP), not EOF | Incorporated | Task 2 places EVIDENCE_MISSING_SAMPLE_CAP at line ~347 area, not end of file |
| OpenCode | Schema field order: timestamp should be last | Incorporated | Task 1 schema places timestamp last, matching existing evidence_missing variant convention |
| OpenCode | Test import line should be added to existing import, not new import | Incorporated | Task 3 specifies adding to existing `audit.ts` import on line 4 |
| OpenCode | Describe block naming convention should match existing pattern | Incorporated | Task 3 uses `describe('buildEvidenceMissingSummary (EVID-03, D-05/D-06/D-07)')` matching PRIO-03 convention |
| Antigravity | Missing `EvidenceMissingSummaryAuditEvent` export | Incorporated | Task 2 exports `EvidenceMissingSummaryAuditEvent = Extract<JobAuditEvent, { stage: 'evidence_missing_summary' }>` |
| Antigravity | Type mismatch: `line` is `number | undefined` in producer, `number | null` in EvidenceMissingEntry | Incorporated | Task 2 EvidenceMissingEntry.line is `number | null`; producer in 21-02 uses `line: line ?? null` |

<tasks>

<task type="auto">
  <name>Task 1: Add evidence_missing_summary schema variant to jobAuditEventSchema (timestamp last)</name>
  <files>src/shared/schema.ts</files>
  <read_first>
    src/shared/schema.ts lines 795-920 (full jobAuditEventSchema + existing evidence_missing variant at lines 907-920)
    src/shared/schema.ts lines 29 (dateStringSchema), 748-749 (fileReviewPassSchema)
  </read_first>
  <action>
    Insert a new discriminated union arm for `evidence_missing_summary` in `jobAuditEventSchema` (schema.ts) immediately after the existing `evidence_missing` variant (which ends with `.passthrough(),` on line 920) and before the `// Phase 18 round/anchor audit events` comment block that follows on line 921.

    The new arm schema uses `z.object({...}).passthrough()` matching every other arm in the union.

    Fields in this exact order (matching existing variant conventions, timestamp MUST be last):
    - `stage: z.literal('evidence_missing_summary')`
    - `file: z.string()`
    - `pass: fileReviewPassSchema` (reuse the exported schema from line 748-749)
    - `absentCount: z.number().int().min(0)`
    - `notInHunkCount: z.number().int().min(0)`
    - `sample: z.array(z.object({ path: z.string(), line: z.number().nullable().optional(), title: z.string().max(100), reason: z.enum(['absent', 'not_in_hunk']) })).max(20)`
    - `timestamp: dateStringSchema`

    Field ordering rule: `timestamp` MUST be the last field. This is NOT cosmetic — downstream parsers that iterate over keys expect a consistent field ordering pattern matching all other variants (evidence_missing at line 912-920 has timestamp last, filtered at line 858 has timestamp last, etc.). List `sample` immediately before `timestamp`.

    The preceding JSDoc block comment must explain:
    - This is the aggregate replacement for per-finding evidence_missing (EVID-03 / D-01..D-04)
    - The legacy per-finding variant above (lines 907-920) is kept forever — old persisted events still parse
    - Counts are camelCase per D-03 (absentCount, notInHunkCount)
    - Sample entry line is nullable and carries the post-orphan-remap line (current behavior) per D-02. Phase 22 (EVID-04) will swap to the pre-remap original line.
    - Sample preserves model emission order per D-07, not grouped by reason
    - Sample entries carry `reason` per D-04 for EVID-02 consumption
    - Privacy-bounded: titles route through redactFindingTitle (max 100 char marker); never body/diff/existingCode/codeSuggestion

    Do NOT modify the existing `evidence_missing` variant in any way. Its lines 907-920 must remain byte-identical. The `evidence_missing_summary` stage literal is distinct and does not collide with `evidence_missing` — Zod's discriminatedUnion uses the `stage` field to select the correct variant.

    Verify via a temporary scaffold test file that is created, run, and then deleted:
    1. Create `test/schema-phase21.spec.ts` with four test cases:
       a. Parse a valid `evidence_missing_summary` event through `jobAuditEventSchema.safeParse` and assert success
       b. Parse a request missing `absentCount` field and assert failure
       c. Parse a legacy `evidence_missing` event shape and assert it still passes (proving the legacy variant is byte-identical)
       d. Parse a request with `sample` of length 21 and assert the schema rejects it (`.max(20)` enforcement)
    2. Run `npx vitest run test/schema-phase21.spec.ts --project node`
    3. Delete the scaffold file: `rm test/schema-phase21.spec.ts`

    The durable schema round-trip test lives in Task 3 (audit-trail.spec.ts). The existing `test/schema-v1.2-contracts.spec.ts` does NOT require modification.
  </action>
  <verify>
    <automated>npx vitest run test/schema-phase21.spec.ts --project node && rm test/schema-phase21.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/shared/schema.ts` contains `stage: z.literal('evidence_missing_summary')` in `jobAuditEventSchema` with fields in order: stage, file, pass, absentCount, notInHunkCount, sample, timestamp
    - `src/shared/schema.ts` existing `evidence_missing` variant (lines 907-920) is byte-identical — zero changes to that block
    - A valid `evidence_missing_summary` event passes `jobAuditEventSchema.safeParse`
    - An event with missing `absentCount` fails parsing
    - A legacy `evidence_missing` event still parses successfully (backward compat proven)
    - A sample array of length 21 is rejected by the schema (`.max(20)` enforcement)
    - `npm run typecheck` passes
    - Temporary scaffold file `test/schema-phase21.spec.ts` does not exist after verification completes
  </acceptance_criteria>
  <done>Schema accepts evidence_missing_summary events with timestamp last; legacy variant unchanged; typecheck passes; scaffold verified and deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Create EvidenceMissingEntry type, EvidenceMissingSummaryAuditEvent type, EVIDENCE_MISSING_SAMPLE_CAP constant, and buildEvidenceMissingSummary builder</name>
  <files>src/server/core/audit.ts</files>
  <read_first>
    src/server/core/audit.ts (full file — imports at lines 1-8, recordUnitAudit at lines 30-60, toAuditIdentifier at 81-87, buildFileSkipEvents at 228-257, Phase 19 ensemble section at 340-413 including sample caps at 345-349 and EnsembleVoteAuditEvent at 351, critic section at 443-491 including CRITIC_DECISION_SAMPLE_CAP at 451 and CriticDecisionsAuditEvent at 453, walkthrough builder at 525-565 ending at line 565)
    src/server/core/audit-redact.ts lines 53-56 (redactFindingTitle)
  </read_first>
  <action>
    In `src/server/core/audit.ts`, add the following components at specific locations. Placement is critical for codebase discoverability.

    1. **`EVIDENCE_MISSING_SAMPLE_CAP` constant** — placed immediately after `ENSEMBLE_FAILED_RUN_REASONS_CAP` (line 349), before the `export type EnsembleVoteAuditEvent` at line 351:
       Add the line: `const EVIDENCE_MISSING_SAMPLE_CAP = 20;`
       Precede it with a comment: `// Phase 21 (EVID-03): evidence_missing_summary bounded sample cap (schema .max(20) constant for the builder).`

       Do NOT place this constant at end of file — it belongs near other sample caps per OpenCode REVIEWS finding (line ~347 area).

    2. **`EvidenceMissingSummaryAuditEvent` type** — placed after the constant, before `buildEvidenceMissingSummary`:
       Add: `export type EvidenceMissingSummaryAuditEvent = Extract<JobAuditEvent, { stage: 'evidence_missing_summary' }>;`
       This follows the precedent of `EnsembleVoteAuditEvent` (line 351), `CriticDecisionsAuditEvent` (line 453), and `WalkthroughEnrichmentAuditEvent` (line 531).

    3. **`EvidenceMissingEntry` interface** — exported, placed after the type, before the builder:
       ```typescript
       export interface EvidenceMissingEntry {
         path: string;
         line: number | null;
         title: string;
         reason: 'absent' | 'not_in_hunk';
       }
       ```
       Field types follow D-10: `line: number | null` (the producer normalizes its `number | undefined` via `?? null` in 21-02). The `reason` field is a union literal matching the schema's `z.enum(['absent', 'not_in_hunk'])`.

    4. **`buildEvidenceMissingSummary` function** — placed after the type/interface exports, before the Phase 20 comment block that precedes `recordWalkthroughAudit` at line 567:
       - Signature: `(file: string, pass: FileReviewPass, entries: EvidenceMissingEntry[]): EvidenceMissingSummaryAuditEvent | null`
       - Returns `null` when `entries.length === 0` (D-05 — no zero-count event for empty input)
       - Computes `absentCount = entries.filter(e => e.reason === 'absent').length` and `notInHunkCount = entries.length - absentCount`
       - Builds a `sample` array by slicing entries to at most `EVIDENCE_MISSING_SAMPLE_CAP` entries (preserving model emission order per D-07 — NOT grouped by reason)
       - Each sample entry applies `redactFindingTitle` to `title` (Phase 20.1 D-06 privacy boundary)
       - Constructs the return value using `satisfies JobAuditEvent` — NOT `as JobAuditEvent`:
         ```typescript
         const event = { stage: 'evidence_missing_summary' as const, file, pass, absentCount, notInHunkCount, sample, timestamp: new Date().toISOString() };
         return event satisfies JobAuditEvent;
         ```
         The `satisfies` operator is a compile-time check. If the literal ever diverges from the `JobAuditEvent` union member, TypeScript rejects at compile time rather than silently widening (Codex REVIEWS finding).
       - JSDoc: Follow the `buildEnsembleVoteAuditEvent` (lines 353-362) / `buildFileSkipEvents` (lines 228-257) convention:
         - Document EVID-03 requirement
         - Document D-05 (null for empty), D-06 (always aggregate), D-07 (preserve order)
         - Document privacy boundary via `redactFindingTitle` matching T-15-04-01 / Phase 20.1 D-06
         - Document `buildFileSkipEvents` as precedent pattern
         - Use present tense, descriptive tone matching existing JSDoc

    No changes to existing imports — `FileReviewPass` is already imported at line 3 (`import type { CriticDecision, FileReviewPass, JobAuditEvent, ReviewSeverity } from '@shared/schema'`).
  </action>
  <verify>
    <automated>npm run typecheck && grep -q "buildEvidenceMissingSummary" src/server/core/audit.ts && ! grep -q "as JobAuditEvent" src/server/core/audit.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/server/core/audit.ts` exports `EVIDENCE_MISSING_SAMPLE_CAP = 20` at line ~347 area (immediately after ENSEMBLE_FAILED_RUN_REASONS_CAP)
    - `src/server/core/audit.ts` exports `EvidenceMissingSummaryAuditEvent = Extract<JobAuditEvent, { stage: 'evidence_missing_summary' }>`
    - `src/server/core/audit.ts` exports `EvidenceMissingEntry` interface with `path: string`, `line: number | null`, `title: string`, `reason: 'absent' | 'not_in_hunk'`
    - `src/server/core/audit.ts` exports `buildEvidenceMissingSummary(file, pass, entries): EvidenceMissingSummaryAuditEvent | null`
    - Builder return value uses `satisfies JobAuditEvent` — grep for `as JobAuditEvent` returns 0 in audit.ts
    - `buildEvidenceMissingSummary(file, pass, [])` returns `null`
    - `buildEvidenceMissingSummary(file, pass, entries)` returns an event with `stage === 'evidence_missing_summary'`, correct `absentCount`/`notInHunkCount`, sample entries with redacted titles, bounded to `EVIDENCE_MISSING_SAMPLE_CAP`
    - `npm run typecheck` passes
  </acceptance_criteria>
  <done>Builder exists with typesafe return via satisfies, correct constants, type exports, null-for-empty, aggregate-for-non-empty; typecheck passes; no as JobAuditEvent cast present.</done>
</task>

<task type="auto">
  <name>Task 3: Add builder unit tests in audit-trail.spec.ts with schema round-trip</name>
  <files>test/audit-trail.spec.ts</files>
  <read_first>
    test/audit-trail.spec.ts lines 1-9 (existing imports) and 228-295 (buildFileSkipEvents describe block — the exact test pattern to follow)
    src/server/core/audit.ts (the builder function that tests will call)
    src/shared/schema.ts (jobAuditEventSchema for schema round-trip test)
  </read_first>
  <action>
    In `test/audit-trail.spec.ts`, add a new `describe('buildEvidenceMissingSummary (EVID-03, D-05/D-06/D-07)')` block at the end of the file (after the `buildFileSkipEvents` describe block which ends at line 295).

    Import changes:
    - Add `buildEvidenceMissingSummary` to the **existing** import on line 4 (NOT a new import statement):
      Change line 4 from: `import { buildFileSkipEvents, recordFileSkips, recordUnitAudit } from '@server/core/audit';`
      To: `import { buildFileSkipEvents, recordFileSkips, recordUnitAudit, buildEvidenceMissingSummary } from '@server/core/audit';`
    - Add a new import line: `import { jobAuditEventSchema } from '@shared/schema';`

    Test cases (per D-09, expanded per REVIEWS.md findings from Codex/OpenCode/Antigravity):

    1. **empty input returns null (D-05)**: `buildEvidenceMissingSummary('a.ts', 'main', [])` is null. Follow `buildFileSkipEvents` empty test at line 267.

    2. **single entry still produces one aggregate (D-06)**: One absent entry returns event with `absentCount=1, notInHunkCount=0, sample.length=1`. The event is an aggregate, not a per-finding event.

    3. **mixed reasons produce correct counts**: Two entries (one absent, one not_in_hunk) return `absentCount=1, notInHunkCount=1`.

    4. **sample capped at 20**: 25 entries produce a sample of exactly 20 entries. `absentCount` and `notInHunkCount` reflect the FULL total (25, not the capped 20).

    5. **model emission order preserved (D-07)**: Entries in order `[absent, not_in_hunk, absent]` yield sample entries in the same order with correct `reason` values.

    6. **privacy boundary — titles redacted (D-04 / Phase 20.1 D-06)**: Entry `title: 'secret-finding'` produces sample entry `title: '[title-redacted]'`.

    7. **file+pass identity**: The event carries the correct `file` and `pass` values passed to the builder.

    8. **both 'main' and 'security' passes accepted**: Passes both `'main'` and `'security'` as the `pass` argument and asserts the event carries the correct pass.

    9. **schema round-trip (Codex REVIEWS finding — ensures no silent malformed events at read time)**:
       Create an event with two entries (one absent, one not_in_hunk). Parse the event through `jobAuditEventSchema.safeParse`. Assert `result.success === true`. This proves the builder's output is valid against the schema.

    Pattern for each test: plain `it(...)` — no DB needed (pure builder), so NO `dbDescribe`. Use `expect(buildEvidenceMissingSummary(...))` patterns matching the `buildFileSkipEvents` tests at lines 228-295.

    Type annotations: Since the builder returns `EvidenceMissingSummaryAuditEvent | null`, extract the non-null event in tests via:
    ```typescript
    const event = buildEvidenceMissingSummary('a.ts', 'main', [...])!;
    expect(event.stage).toBe('evidence_missing_summary');
    ```
    The `!` non-null assertion is safe because the test supplies non-empty input.

    Assertion style: Use concrete property access for the narrowed type — `event.absentCount`, `event.notInHunkCount`, `event.sample[0].title`, `event.file`, `event.pass`. Avoid `(event as any)` or dynamic access patterns.
  </action>
  <verify>
    <automated>npx vitest run test/audit-trail.spec.ts --project node -t "buildEvidenceMissingSummary"</automated>
  </verify>
  <acceptance_criteria>
    - `test/audit-trail.spec.ts` contains `describe('buildEvidenceMissingSummary (EVID-03, D-05/D-06/D-07)')` block after the `buildFileSkipEvents` block (after line 295)
    - `buildEvidenceMissingSummary` added to the **existing** import on line 4 (not a new import statement)
    - `jobAuditEventSchema` imported from `@shared/schema` as a new import line
    - Test "empty input returns null (D-05)" passes
    - Test "single entry still produces one aggregate (D-06)" passes
    - Test "mixed reasons produce correct counts" passes
    - Test "sample capped at 20" passes
    - Test "model emission order preserved (D-07)" passes
    - Test "title redacted via redactFindingTitle" passes
    - Test "file+pass identity correct" passes
    - Test "both 'main' and 'security' passes accepted" passes
    - Test "builder output round-trips through jobAuditEventSchema" passes (Codex REVIEWS finding)
    - `npx vitest run test/audit-trail.spec.ts --project node -t "buildEvidenceMissingSummary"` exits 0
  </acceptance_criteria>
  <done>All builder tests pass including schema round-trip; buildEvidenceMissingSummary is fully unit-tested with no as-JobAuditEvent gap; imports follow codebase conventions.</done>
</task>

</tasks>


<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Builder -> Schema | Pure function producing synthetic event; `satisfies JobAuditEvent` enforces compile-time type safety; schema round-trip in tests validates at runtime |
| Producer -> Audit DB | The builder's output enters the ring buffer via recordUnitAudit; non-blocking append |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-01 | Information Disclosure | buildEvidenceMissingSummary sample entries | low | mitigate | All sample titles pass through redactFindingTitle (Phase 20.1 D-06) before storage; the builder never includes body/diff/existingCode/codeSuggestion in the event payload. Matches existing T-15-04-01 convention. |
| T-21-02 | Denial of Service | jobAuditEventSchema new arm | low | mitigate | The new variant uses .passthrough() so extra fields from future phases (Phase 22 EVID-04) do not cause parse failures. The per-element READ pattern (established Phase 13) ensures one corrupted event never erases the whole trail. |
| T-21-03 | Spoofing | EvidenceMissingEntry accumulation | low | accept | The producer is the sole accumulator; entries are built from already-validated model output. A compromised model could fill the accumulator, but the builder caps the sample at 20 and the ring buffer limits total events. |
| T-21-07 | Tampering | Malformed builder output (cast bypasses schema) | low | mitigate | Builder uses `satisfies JobAuditEvent` (compile-time check) NOT `as JobAuditEvent` (silent widening). Schema round-trip test validates at test time. Dual mitigation prevents malformed events reaching the ring buffer. |
| T-21-SC | Tampering | npm/pip/cargo installs | high | mitigate | No installs this phase; skip. |
</threat_model>

<verification>
- `npx vitest run test/audit-trail.spec.ts --project node -t "buildEvidenceMissingSummary"` exits 0
- `npm run typecheck` passes
- `grep -c "as JobAuditEvent" src/server/core/audit.ts` returns 0 (no silent widening cast used)
- Schema accepts valid evidence_missing_summary events and rejects invalid ones
</verification>

<success_criteria>
- jobAuditEventSchema accepts evidence_missing_summary events (timestamp last), keeps legacy evidence_missing variant byte-identical
- EvidenceMissingEntry and EvidenceMissingSummaryAuditEvent types exported from audit.ts
- EVIDENCE_MISSING_SAMPLE_CAP constant placed at line ~347 area (immediately after ENSEMBLE_FAILED_RUN_REASONS_CAP)
- buildEvidenceMissingSummary uses `satisfies JobAuditEvent` (not `as`), returns correct aggregate or null
- Builder unit tests pass with coverage for empty, single, mixed, capped, order-preserving, privacy, identity, pass-variant, and schema-round-trip cases
- No `as JobAuditEvent` cast in the builder function body
</success_criteria>

<output>
Create `.planning/phases/21-evidence-missing-aggregate-summary-event-schema-audit-writer/21-01-SUMMARY.md` when done
</output>
